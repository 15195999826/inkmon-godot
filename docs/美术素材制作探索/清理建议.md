# `_candidates` 清理建议

当前状态：

- `docs/美术素材制作探索/` 保存长期结论、关键图和关键 JSON。
- `docs/美术素材制作探索/原始候选完整归档/` 保存最后保留的完整候选目录。
- `blender/textures/_candidates/` 已清空，只作为后续新实验的临时输出缓冲。

## 后续策略

- 新实验继续输出到 `blender/textures/_candidates/<run>`。
- 需要长期保留的结果再迁入 `docs/美术素材制作探索/`。
- 旧 recipe / 旧样本路径统一走 `blender/scripts/texgen/archive_paths.py`。
- 不再把 `_candidates` 当长期存档。

## 已完成

- `.codex-goal/` 已删除，结论已蒸馏入本目录。
- `_candidates` 旧探索目录已删除。
- `_candidates` 最后剩余的完整候选已迁入 `原始候选完整归档/`。

