# 玩法系统实现路线图（gameplay systems roadmap）

> 本文 = 从 [`game-vision.md`](game-vision.md) 的游戏循环推导出的**玩法系统分解 + 实现难度 + 建议顺序**。
> 与 [`main-game-architecture.md`](main-game-architecture.md) 的分工：那边只写**现状真相**，本文写**要去哪、先做什么**。
> 难度评估基于 2026-06-11 代码库现状（统一 live-actor / render2d 统一管线 / 回放冻结世界已落地）；难度是工程性质判断，**不是排期承诺**。内容量（物种/技能/数值）不在本文范围。

---

## 1. 动工前的前置决策（必答，每个一轮短 grill）

| # | 决策 | 为什么必须先做 | 现状 |
|---|---|---|---|
| P1 | **存档语义 vs run 张力** | 远征中任意存读档可废掉"不能回头/丢收获/概率捕捉"全部张力（双评审交叉发现，见 game-vision §1 末）。候选：远征中禁手动存档 / run 内自动快照 / 接受 scum。**决定远征状态的数据形状**，先于远征 flow 实现 | ✅ 已定（2026-07-04）：v1 出征（mission）态 transient 不序列化，出征中禁手动存档 + 出发自动写据点档，据点档现状不动；术语 glossary §4.8，详见 game-vision §1 末 |
| P2 | **三地图语境共存** | 据点格 / 远征大地图 / 战斗棋盘三个空间语境在唯一 world GI 里怎么摆 | ✅ 已定（2026-07-04）：大地图不进 GI grid 机器——世界地理（开档一次生成固定，入据点档 data shape）+ 趟内节点图（接委托蔓延生成，住 MissionState transient）；据点/战斗双 grid 现状不动，§9 触发条件实未命中；见 glossary §4.9 |
| P3 | 捕捉形态三选一（纯结算 / 气绝制 / 满注制，见 game-vision §3） | 可推迟到捕捉系统动工时；框架（战后捕捉）已锁定不影响其它系统 | 可缓 |

## 2. 系统分解 × 已有底座 × 难度

| 系统 | 已有的底座 | 缺的部分 | 难度 |
|---|---|---|---|
| 远征地图两层生成（世界地理 + 趟内 DAG，P2 拍板） | hex grid 插件、迷雾可用 render2d 做；节点图不进 GI grid 机器（无需 astar/占格） | 趟内 DAG 生成器（尖塔式 layered graph，v1 实做）+ 世界生成器（v1 极简/预制占位，格式按入档定死）+ hex 皮肤最简摆 | v1 部分 **低-中**；走廊质量/多区域打磨 = Phase 5+ |
| 三地图语境共存 | 双 grid 切换（据点/战斗，临时方案） | ✅ P2 已定（glossary §4.9）：剩世界地图 data shape + MissionState 两个数据类落地 | **已消解（低）** |
| 任务系统 | NPC handler 模式、command/intent 流（训练战 intent 是现成模板） | 任务数据形状 + 公会 handler 扩展 + 目标格揭示 | 低-中 |
| 野群生成（带个体 roll） | `adopt_unit` 的 roll 机器、species catalog、4v4 用 roster slice 现成 | 按区域生态配野群、临时野生 actor 生命周期 | **低** |
| 战后捕捉（纯结算/气绝档） | downed 语义（HP=0 留 registry）、入队机器（adopt 同款） | 概率公式 + 结算 UI | **低** |
| 战后捕捉（满注档，将来） | **击倒方式数据天然已录在 replay timeline**（record-then-playback 红利：战斗逻辑对捕捉零知觉，捕捉从录像读参数） | 从录像读参数 + 逐只投球交互 | 中 |
| 补给钟 | movement tick 系统 | 每步扣粮 + 粮尽掉血规则 + HUD 一行 | **极低** |
| 远征 flow（出发/途中/回城） | `_replay_active` 冻结同款模式（Host flow 状态 gate 世界泵） | 远征态进出 + 与存档语义（P1）整合 | 低-中 |
| 战斗呈现（combo/高光可见，game-vision §2 想象兑现） | render2d 统一管线、visualizer 槽位制（active 4 / dormant 9，JIT 补） | 逐个补 visualizer + 高光强调演出 | **持续投入型**，单笔都小 |
| 异步 PvP（长线） | 决定性 sim + 录像两端各播，机制天然支持 | 服务端撮合 | 大，正确地排在长线 |

## 3. 三个关键工程判断

1. **远征地图生成两层拆分（2026-07-04 修订，原"手摆 3-5 张固定地图"作废）**：世界地理 = 开档一次生成永久固定（v1 生成器极简或预制图占位，**数据形状按"生成结果入档"现在定死**，不烧桥）；趟内选路 DAG = 接委托后蔓延生成（尖塔式 layered graph 成熟套路，v1 实做，固定 seed 即手摆兜底）；hex 皮肤 v1 最简摆（直线走廊 + 单一地貌）。选路体验参数（一次出征有效决策 10-20 次）在 DAG 生成参数上调；走廊质量/区域生态/多区域 = Phase 5+。
2. **P1/P2 在变贵之前做掉**：都不是重构，是"已知欠着的决定"；写远征代码之后再定会返工。
3. **战斗呈现走 JIT 增量**：visualizer 槽位制天然支持"逻辑层出一个新事件机制 → 补一个三件套"；高光强调（超常发挥/combo 时刻）按 game-vision §2 想象兑现三推论投入，不做一次性大投入。

## 4. 建议实施顺序（顺序可调）

```
Phase 0  前置决策：P1 存档语义 + P2 三地图语境 —— ✅ 完成（2026-07-04 grill；
         含术语 mission/quest、出征两出口、世界地理一次生成固定，glossary §4.8/4.9）
Phase 1  出征骨架：世界地图 data shape（v1 极简生成）+ 出征 flow（两出口）
         + 趟内 DAG 生成（最简皮肤）+ 补给钟 —— 走通「出发 → 走路 → 回家」
         ✅ 已实现（2026-07-04，27 smoke 全绿；尾项/偏差见 plan §5）待用户手感验收
Phase 2  遭遇与捕捉：野群生成 + 4v4 接入出征 + 战后捕捉最简档（P3 此时定）
         —— 走通完整核心循环（可给陌生人玩的最小切片）
Phase 3  委托系统（quest）：公会委托板 → 主委托目标揭示 → 完成判定 + 副委托
         —— 出征有了名字
Phase 4  选路体验：明牌标记 / 迷雾 / 侦察（含个体侦察特性钩子）+ 节点类型
         （事件 / 资源 / 精英）
Phase 5+ 持续层：战斗高光呈现 · 捕捉满注化 · 图鉴 · 段位 gate · 生成器打磨
         （世界/走廊质量/多区域）· 异步 PvP（长线）
```

> v1 切片定义见 game-vision §2/§3/§6（一据点 + 固定世界一个区域 + 趟内生成节点图 + 占位主委托 + 战后捕捉最简档 + 可跳过回放 + 据点存档）。Phase 2 结束 = 切片可玩。
> **工程切分与各 Phase 动工前待拍板问题（Q 编号）见 [`plan/mvp-phase-plan.md`](plan/mvp-phase-plan.md)**（2026-07-04；Phase 1 无待拍板项已开工）。
