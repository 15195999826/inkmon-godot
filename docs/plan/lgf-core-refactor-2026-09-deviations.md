# LGF core 重构 2026-09 — 偏离记录

> 计划正文 `lgf-core-refactor-2026-09.md` §6 的偏离记录全部住这里。存量 ①–㊿（P1–P5）按原格式「阶段 / 条目 / 做了什么 / 为什么 / 影响」原样搬入；P5.5 起新条目按 §2 精简格式追加：`- P<n>-<序号> / 改法 N / 做了什么 / 为什么`，每条不超过 200 字，「影响」只在会改变后续阶段时写，锚点写符号不写 `file:line`，`[假设]` 标计划没写到的选择。文末「已关闭的后续观察」收 §6 后续观察里已标「本条关闭」的条目。

## P1–P5（存量 ①–㊿）

- ① P1 / 提交协议 1 / `origin/master` 已是 `8e56bba`（用户 09-07 的 merge，带入 7 月上游 LGF 提交：`can_activate` 纯查询、`DecisionOutcome`、`add_system` (priority, 注册序) 双键、execution 取消清理 + `on_cancel`、loop 余量结转、同刻 tag 定义序、`EventCollector.push` 深拷贝），不是计划写的 `a4730a1`。做法：本地 `master` 建在 `8e56bba`，改任何代码前先跑 §2 全量组（88 scene）全绿，主仓指针分两步——先单独 `chore: bump addons to 8e56bba`（`bbe5d9a7`），再 P1 commit；push 按协议「不是则停下问用户」留给用户确认后执行（顺序 addons 先、主仓后）。/ 为什么：`origin/master` 是唯一可 push 的基线，在 `a4730a1` 上做完再合并会在 P1 同一批文件（execution instance / component / 测试）上冲突。/ 影响：inkmon 首次钉住 7 月上游改动；`smoke_battle_golden` 指纹不变；后续阶段的行号引用以 `8e56bba` 之后为准。
- ② P1 / 改法 2 / 上游在 `8e56bba` 新增的 registry 依赖一并删除：`ActivateInstanceComponent` / `ActiveUseComponent` 的 `_is_timeline_available()`、`Ability.activate_new_execution_instance` 的 `TimelineRegistry.has` 守卫（原返回 null）、`AbilityActivationQuery.FAILED_TIMELINE` 与 `active_use_query_test._test_missing_timeline`（概念随 registry 消失，删用例）；config 的 `timeline_id` 字段整个删除、不留派生属性，3 处读者改读 `timeline_data.id` / `timeline_data.total_duration`（`SkillValidator` ×2、`skill_preview_validation`）。/ 为什么：计划按 `a4730a1` 写，看不到上游新增点；「一种拼写」优于再造一个只读投影。/ 影响：无运行时差异（timeline 缺失路径已由 build 期 crash 覆盖）。
- ③ P1 / 改法 7 / 泄漏直方图只能按原生类计数：Godot 4.6 `--verbose` 的 `Leaked instance:` 行只带 `RefCounted` / `Node` / `GDScript` / `WeakRef` 等原生类名，不带脚本类名，LGF 类逐类计数不可得；四份基线 + `README.md` 已提交，验收判据改为「原生类计数不增（P1 后四份与基线逐字节一致）+ 释放测试 weakref 断言」。/ 为什么：引擎输出格式限制。/ 影响：后续阶段沿用同一判据；LGF 对象释放的精确仪器只有 `refcount_release_test.gd`。
- ④ P1 / 测试 / 上游新增的 3 个 loop 测试、2 个 execution 测试与 `active_use_query_test` 夹具一并迁直传（计划列 5 / 7 处，实际 8 / 9 处 + 1 个新文件）；`timeline_test.gd` 除计划要求的定义序测试（上游已有 `_test_sorted_tags_tie_break`）外补一条纯时间序测试，顶替原 `_test_register` 顺带覆盖的 `get_sorted_tags` 基本路径；`tests/skill_validator/smoke_skill_validator.gd` 加了可选的 `result.timeline` 恒有值断言。/ 为什么：覆盖不缩水。/ 影响：无。
- ⑥ P1 / 验收第 3 关 / `/code-review max` 十角度跑完后合入 20 处修复，其中两处**超出计划的 hex-only 范围**：把 timeline 静态检查收成 core 的 `AbilityConfig.lint_timelines(configs)`，并新增 `inkmon/tests/smoke_skill_manifest_lint`（注册为 `inkmon/skills` 组）。/ 为什么：D9 原文只说「降级为 hex manifest lint」，但五个角度独立指出——registry 一删，主游戏与 dota2 的 timeline id 唯一性、`validate()`、tags 冻结三项检查全部落空（`collect_timelines()` 在主仓零调用点），复制技能文件忘改 `TIMELINE_ID` 会让两个技能在录像里共用一个 `timelineId` 且全程零报错。守卫只长在 hex 一家，等于主游戏裸奔。/ 影响：core 新增一个 static 查询 API（不进存档、不参与执行期）；inkmon 测试组多一个 ~1s 的 smoke，自带「注入同 id 异实例必须报红」的自证；dota2 只有单个 ability、无 manifest，仍未覆盖（见后续观察）。
- ⑦ P1 / 验收第 3 关 / review 抓到两处**由前一轮 review 修复自己引入的回归**，已在同一轮内改掉：① efficiency 角度建议把 lint 的 `validate()` / 冻结检查挪进「首次见到该 id」分支省重复，但这样一来同 id 的**第二个**实例（正是最该查的那个）永远不被校验，且覆盖面变成依赖 manifest 的 append 顺序——改为按**实例**去重（`get_instance_id()`），共享节奏仍只查一次，异实例一个不漏；② conventions 角度要求 `var frames: Array` 补元素类型，但 `battle_finished` 未发时 `Dictionary.get` 返回的是无类型空 `Array`，直接赋给 `Array[Dictionary]` 会抛类型转换错、把干净的断言失败换成引擎报错——改用 `frames.assign(...)`。/ 为什么：记录下来是因为这类「按单一角度的局部建议改，破坏另一维度」的模式会在后续阶段重演。/ 影响：无。
- ⑤ P1 / 文档与 skill / `/update-lgf-skill` 命令本机不可用（`grep -P` 在当前 locale 不支持；其 diff 路径 `addons/logic-game-framework/` 对主仓只是 gitlink；`update.json.last_commit` 记的主仓 SHA 对应的 addons 指针 `13615b2` 已随 Tiny Swords 历史清理消失），改为手动等价执行：以日期等价基线 `e583e80..60d2318` 的 addons diff 增量更新 `enforcing-lgf/reference/*.md` 并镜像到 `.agents/`，`update.json.last_commit` 记 P1 主仓 commit。/ 为什么：命令坏了，不在本阶段顺手修。/ 影响：记入后续观察待修。

- ⑧ P2 / 改法 1「可选吸收 team_id」/ 吸收了 `team_id` + `set_team_id` / `get_team_id` + `_get_team_int`，并给 hex `EnvironmentActor` 补了一句显式 `_get_team_int() -> 0`。/ 为什么：三家子类的 `_get_team_int()` 逐字相同（都返回 team_id / 未入队 -1），不吸收就是三份副本；但 `Actor` 的默认实现是解析字符串 `_team`，从不 `set_team_id` 的 hex 环境物靠它录成 `0`，基类直接返回 `team_id` 会把它们悄悄变成 `-1`（表演层按 team 上色）。1 处显式特例换掉 3 份复制。/ 影响：全仓唯一真的变了 recorded team 的是 inkmon 玩家 / NPC（`InkMonWorldActor` 现在是 `BattleActor`，0 → -1）——它们被 `should_record_actor` 挡在录像外，当前不可观测，已在 `ink_mon_world_actor.gd` 类注释里记下。
- ⑨ P2 / 行为变化「dota2 录像接受」/ 计划写「若 frame 体积成问题再覆盖回空 `setup_recording`」，实际改为 `Dota2AutoBattleProcedure._recording_enabled = false`（整个不建 recorder）。/ 为什么：三个 review 角度独立指出这不只是体积——① `attributeChanged` / `tagChanged` 与业务事件共用同一个 `event_collector`，会把 lane 场景那 14 行 debug 面板（M1 垂直切片的肉眼验收通道）里的 `damage` / `DIED` 挤出去；② 死兵走 `remove_actor` 而 recorder 没有对应的 `unregister_actor`，订阅会留到战斗结束；③ dota2 的录像本就播不了（开战快照在 spawn 之前拍恒空、无 `map_config`、无 `positionFormats`，`finish()` 返回值四个调用点全丢弃）。覆盖回空 `setup_recording` 只治 ①，治不了 ③ 的「录了也没用」。/ 影响：dota2 泄漏直方图 590 → 170 RefCounted、66 → 61 GDScript，基线文件已按「下降要棘轮」更新（见 leak-baseline `README.md`）；`Dota2LogicFrame.events` 回到 P2 之前的内容。
- ⑩ P2 / 完成定义「三家 actor 基类各不再定义 `get_ability_set`」/ 三家仍各自定义 `get_ability_set()`（以及 `get_attribute_set()` / `get_attribute_snapshot()`）。/ 为什么：这三个正是 D4 指定的协变收窄出口——`HexBattleActor.get_ability_set() -> BattleAbilitySet` 让 hex 代码不必到处强转；`get_attribute_snapshot()` 各家录的字段本就不同（hex 只 hp/max_hp，inkmon 六维 + 身份），用 core 的全属性默认会改录像内容。真正下沉的是 `_is_dead` / `check_death` / `is_dead` / `mark_dead` / `is_pre_event_responsive` / `_on_id_assigned` / `setup_recording` / `get_ability_snapshot` / `get_tag_snapshot` / `serialize` / team 四件套。/ 影响：完成定义这条按「骨架成员」而非「所有同名方法」判定，CHANGELOG 已写明例外。
- ⑪ P2 / 改法清单外的 core 新增 / 加了三个计划没列的 core API：`BattleActor._hp_source()`（血条来源钩子，`has_hp` / `get_current_hp` 共用）、`BattleActor.set_death_latch(value)`（唯一解闩入口）、`Ability.has_executing_instance()`（不分配版本），并把 `RawAttributeSet._snapshot_all_values()` 提为 public `snapshot_current_values()`。/ 为什么：`_hp_source` 让「没血条」与「血条为 0」在一处分开（纯数据 actor 不被判成尸体）；`set_death_latch` 是因为 inkmon `sync_downed_state()` 要把闩拨回 false，没有这个入口它只能跨 submodule 直写基类私有字段 `_is_dead`；`has_executing_instance()` 是因为 `get_executing_instances()` 每次 `filter` 出一个新数组，而主循环每 actor 每 tick 只想知道有没有；`snapshot_current_values` 是因为 `BattleActor.get_attribute_snapshot()` 与 raw 内部的 before/after 对比需要同一份快照定义。/ 影响：core 多四个小 API，全部有测试覆盖。
- ⑫ P2 / 验收第 3 关 / `/code-review max` 十角度合入 20 处修复，其中三处**超出计划改法清单**：① `EventProcessor.process_post_event` 的 `assert_crash` 之后补 `continue`（assert 在 debug 下不接管控制流，不 continue 会紧接着对 null 调 `receive_event`，把清楚的报错埋进无关的引擎报错）；② hex `hex_battle_shield_resolver` ×2 与 `hex_battle_damage_utils` ×1 的 `"ability_set" in actor` 一并改真类型（完成定义只 grep `core/ stdlib/`，这三处本会逃掉，而它们「探不到就当没盾」是静默失效）；③ `GameWorld.get_actor()` 改走新的 `get_instance_of_actor()`，`{instance_id}:{local_id}` 的解析收成一处。/ 为什么：都是本轮「真类型化」自己该有的收尾，留着就是同一份改动里两套口径。/ 影响：无行为变化，测试全绿。
- ⑬ P2 / 验收第 3 关 / 修复轮自身引入并当场改掉的两处（延续 ⑦ 那个模式）：① 为省一次虚函数调用，`check_death()` 一度直接读 `_hp_source()` 而不走 `has_hp()` / `get_current_hp()`——这让「换属性名覆盖这两个虚函数」的合同当场失效（覆盖了也不生效），改回经两个虚函数取值，注释按真实合同重写；② 新加的 `_test_setup_recording_full` 为算期望条数多注册了三组 `RecordingUtils` 订阅却不退订，它们捕获的 `ctx → BattleRecorder` 活到进程结束，core 泄漏直方图当场 +5 RefCounted / +8 GDScript——加 `_drain()` 注册后立即退订，直方图恢复与基线一致。/ 为什么：这两条都是「按单一维度优化，破坏另一维度」，与 ⑦ 同型，值得继续记。/ 影响：无。
- ⑭ P2 / 文档与 skill / `/update-lgf-skill` 仍不可用（见 ⑤），继续手动等价执行：按 P2 实际改动的 API 面增量更新 `enforcing-lgf/reference/{entity,abilities,attributes,stdlib,conventions-detail,example-app-game-logic}.md` 与 `gdscript-coding/SKILL.md`，镜像到 `.agents/`（`diff -r` 逐字节一致）。`gdscript-coding` 的 `I*` 范例原本用 `IAbilitySetOwner`（本阶段删除），换成真实的 `IGameStateProvider`——首版是凭印象手写的，与真实实现有两处出入（丢了 `provider is Object` 守卫、兜底从 `Time.get_ticks_msec()` 变成 `0.0`），已按源码逐字符改正。`update.json.last_commit` 改记 addons SHA（按 §6 后续观察里对该命令的修复方向）。/ 为什么：命令坏了，不在本阶段顺手修。/ 影响：同 ⑤。

- ⑮ P3 / 改法 B2「41 处机械改 `ctx.instance`；新代码约定每项目一个 `world(ctx)` helper（hex、dota2 / inkmon 对偶）」/ 41 处先全部机械改成 `ctx.instance`，其中**原本不判空、拿到就解引用**的 6 处（hex `start_move` / `apply_move` / `push` / `reflect_damage` / `damage` / `heal`）改走 `HexBattleGameStateUtils.world(ctx)`；**inkmon / dota2 不加 helper**。/ 为什么：两家的全部读点（inkmon 8 处、dota2 1 处）原本都判空降级（静默跳过、返回空结果或 failure），没有一处「必须有世界」——加 helper 要么零调用，要么把它们的降级改成断言。首轮曾把 inkmon 移动 ×2、dota2 伤害 3 处改走新建的 `InkMonBattleGameStateUtils.world` / `Dota2GameStateUtils.world`，并误记为「原本不判空」，review 指出后撤回（见 ㉑）。helper 只收拢「拿到就解引用」的读点：它们遇到 null 本来就会在下一行崩，换成 helper 只是把引擎报错换成带类型名的断言，非 null 路径逐位不变；先判空再降级的读点保留 `var battle: X = ctx.instance`——两种写法各对应一种语义，不是同一语义两套拼写。/ 影响：hex 6 处的 null 报错更清楚；inkmon / dota2 行为与 P3 之前一致。`docs/README.md` §4 与 skill 写明「项目出现第一处必须有世界的读点时再加 helper」。
- ⑯ P3 / 改法 B1「`TagAction._get_logic_time` → `ctx.instance.get_logic_time()`（null 则 0.0；删除挂钟 fallback）」/ 整个函数删除。/ 为什么：它全仓零调用（`@deprecated` 的 `TagAction` facade 里遗留的私有 helper），按计划改写等于给死代码续命；删掉后 core 对 `IGameStateProvider` 的最后一处引用随之消失，挂钟 fallback 同样不复存在。/ 影响：无。
- ⑰ P3 / 改法 B1「`Ability._build_remove_context` 与 `PreEventComponent._rebuild_context` 用 `actor.get_owner_gameplay_instance()`」/ 两处都改为先 `GameWorld.get_instance_of_actor(owner_id)` 反查 instance、再 `instance.get_actor(owner_id)` 取 actor；`AbilitySet.receive_event` 每次投递只反查一次，本轮所有 ability 的 context 共用。/ 为什么：原先的 `GameWorld.get_actor()` 内部本就先做这次反查，再对 actor 调 `get_owner_gameplay_instance()` 是对同一个 id 第二次解析；这样五个填充点完全同形，正是 D2 说的「同一机制」；post 广播是 actor × ability 的热路径，逐 ability 反查是白做的字符串解析。/ 影响：owner 已注册时与计划写法得到同一 instance；唯一差别在「actor 已从 instance 移除、但 id 前缀仍指向活着的 instance」（dota2 死兵 `remove_actor` 之后）——此时 remove context 带 instance、actor 相关字段为 null，计划写法会连 instance 一起给 null。现有 on_remove 消费者只读 attribute_set / ability_set，不可观测。
- ⑱ P3 / 文档「`action-architecture.md` §0.6：lifecycle action 现可读 `ctx.instance`，仍不许 push 事件」/ 只写了「拿到真 instance、定位不变（不发 projectile / 不移动 actor / 不结算伤害）」，没写「不许 push 事件」。/ 为什么：hex stun / silence / break buff 的 on_apply 本就用 `StageCueAction` 往 event_collector push 表演 cue，写「不许 push 事件」会与现役合法用法矛盾；原文的边界本来就是「不发 projectile、不移动 actor」，沿用。/ 影响：无。
- ⑲ P3 / 测试「更新 8 个构造 context 的 core 测试；`flow_action_test` 等传 `GameplayInstance.new("t")`」/ 实际改了 14 个测试文件（另含 `timeline_loop` / `ability_execution_instance` 的 `tick` / `fire_sync_actions` 去参，`pre_event_component_test` 删掉失去用途的 `MockState`，`event_processor_test` 删 `mock_state`），并新增 `instance_context_test.gd`（5 用例，登记 `TEST_PATHS`）；释放测试除计划点名的 NoInstance ExecutionContext + AbilityLifecycleContext 外，多挂一个 timeline tag action 的 ExecutionContext 探针（对应循环引用审查清单 ②），且每个探针先断言 context 确实带着 instance，释放断言前先确认 key 真被捕到。/ 为什么：计划只要求改构造点，但「五个填充点都拿到 owner 的 instance」与「grant 恒投递」是本阶段的新合同，没有直接测试就只能靠全量组间接兜；探针不先确认捕到，「没跑到」会被当成「已释放」而假绿。/ 影响：core 单测 187 → 192。
- ⑳ P3 / 文档与 skill / `/update-lgf-skill` 仍不可用（见 ⑤），继续手动等价执行：按本阶段 API 面更新 `enforcing-lgf/SKILL.md`（§4 整节改写并去掉「Variant 是有意的鸭子类型，不要 fix」、§7 补一句、检查清单末条替换）与 `reference/{abilities,events,actions,conventions-detail,entity,stdlib,cast-eligibility-vs-condition,example-app-game-logic}.md`，`gdscript-coding/SKILL.md` 的 `I*` 范例——仓内已无活的 `I*` 静态探针类，改成明示「模板」的 `ISaveable`，并在「有了公共基类 `I*` 就退役」一节补上 `IGameStateProvider` 这第二个实例，`_` 前缀范例同步去参；镜像到 `.agents/`（`diff -r` 逐字节一致）；顺手改掉 `stdlib.md` 目录里 P1 残留的「Timeline Autoload」。`update.json.last_commit` 记本阶段 addons SHA。/ 为什么：命令坏了，不在本阶段顺手修。/ 影响：同 ⑤。
- ㉑ P3 / 验收第 3 关 / `/code-review max`（addons 路径目标，finder 同时读了 inkmon diff；主仓工作区的 skill / plan / `scripts` 改动留到复审轮一并审）十角度 → 22 条验证 → 查漏，报 15 条，修复另起 addons commit：① `GameWorld.create_instance` 的 factory 里调 `start()`（`scripts/SimulationManager.gd`、hex `demo_headless.gd`、`skill_scenario_harness.gd`）改为先注册再 `start()`——P3 前这些调用方给 grant 显式传 `self`，P3 后 start 期间按 owner id 反查只得 null（当前无受害读者；P4 从 instance 取 processor / collector 后会静默丢），写法照 `demo_frontend` / `skill_preview`；② 撤回首轮 3 处判空读点的断言化（见 ⑮），`cancel_move` 的 `as` + failure 也收成判空写法，`Dota2GameStateUtils` 与 `InkMonBattleGameStateUtils.world` 随之删除；③ 删 `demo_headless.gd` 零调用、从不注册 instance 的静态 `run_battle()`；④ `ActorId.extract_instance_id` / `extract_local_id` 不再经 `parse()` 分配 Dictionary（P3 让它进了派发 / 建 context 的热路径）；⑤ 释放测试补 NoInstance on_apply action 的 context 探针，文件头只声称真正探到的四处；⑥ 文档：「隐式下转类型不符即硬错」改为只在 debug 构建检查（`execution_context.gd` / CHANGELOG），`HandlerContext.game_state` 删除条补 why，三处残留的 provider 尾参签名（`hex_battle_damage_utils.gd` 头注释、skill `example-app-game-logic.md`、CHANGELOG P2 条目加后向注），`target-selector.md` 自定义选择器示例改真实 API（`Array[String]` + `filtered()`），装备容器头注释删掉解释不存在字段与「现役都没声明」的句子，`gdscript-coding` 的 `I*` 节不再说「仓里 `I*` 类都已退役」（inkmon `IWorldQuery` 是 facade）且 `ISaveable` 模板的 fallback 先响亮报错；⑦ 删 provider 去参留下的死参数（`push_action._grant_displacement_action_lock` / `spawn_fire_tile_action._initialize_fire_tile` 的 `battle`；测试 override 的 `ctx` 加 `_` 前缀）与 heal / damage 里多余的判空、二次收窄。/ 有意不修：hex / inkmon `broadcast_post_damage` 的 `battle` 参数本阶段未用，但 P4 C1 要把其中的 `GameWorld.event_processor` 换成 `battle.event_processor`（P5 目标签名也保留它），现在删了下一阶段要加回；`instance == null` 合法（孤立测试 / 未注册 owner）按 P3 改法 B1 保留，见后续观察。/ 为什么：都是 P3 的去参 / 按 id 反查引入或暴露的问题，留着就是本阶段交付物自带的缺口。/ 影响：inkmon / dota2 行为回到 P3 之前（首轮的断言化撤回）；harness / demo 的 start 期 grant 重新拿到真 instance；core 单测数不变（192）。
- ㉒ P3 / 验收第 3 关复审 / `/code-review max` 复审（addons `origin/master..HEAD` + 主仓工作区全量，含首轮没审到的 skill / plan / `scripts`）十角度 → 23 条候选分 9 组验证（17 条成立、1 条存疑、5 条驳回）→ 查漏，修复另起 addons commit：① **world ↔ procedure 强引用环**：hex / dota2 / inkmon procedure 子类的 `_world_instance` 是强引用字段，与 `WorldGameplayInstance._active_battle` 成环——`_active_battle` 只在 `world.tick()` 收尾时置空，直接驱动 procedure 结束（dota2 smoke / 前端）或战斗中途 destroy 时，整场 world / actor / ability / execution 留到进程退出；P3 写进 CHANGELOG / README / skill 的「引用图不成环」证明因此对子类不成立。改为经基类 `_world`（WeakRef）的只读属性（调用点不变；复审第三轮再改为协变覆盖 `_get_world()`，见 ㉓），dota2 泄漏直方图从 170 RefCounted / 61 GDScript / 9 WeakRef / 1 GDScriptNativeClass 清零并棘轮。开着录像的战斗中途 destroy 仍会经 recorder 订阅闭包泄漏 recorder 与它订阅的全部 actor（不止参战者，第四轮更正），不在此修复内（见后续观察）。② `GameWorld.create_instance` 写明「factory 只构造、注册后再 start」，三处复制的调用点注释删掉，dota2 两处 factory 内 `start()`（`dota2_lane_battle.gd`、`smoke_lane_wave_engage.gd`）一并改成先注册。③ `ActorId.parse()` 改由两个 `extract_*` 拼出，拆分文法只剩一份。④ 文档口径对齐：skill `actions.md` 的「一律 world(ctx)」、`entity.md` 的「extract 经 parse()」、`ability_lifecycle_context.gd` 的「应 assert_crash」、README §4 两处注释、CHANGELOG 的探针描述；「instance 填充点」清单（README §4 / conventions-detail / CHANGELOG / 测试头）补上 `AbilitySet.can_activate` 并新增对应用例；README §4 示例恢复「业务逻辑前快照 alive_actor_ids」（P3 改写时丢了，照抄会让被击杀目标收不到自己的 post）；P4 C1 里被 ⑰ 改过时的指令与行号刷新（否则 P4 会把「每次投递只反查一次」退化成逐 ability 反查）。⑤ `gdscript-coding` 的 `ISaveable` 模板：`is_instance_valid` 守卫（freed 对象上 `is Object` 会报错）、`% [obj]`（Array 参数会被当成格式参数表）。⑥ 小清理：damage_action 击杀判定复用已收窄的 `battle`、harness 不可达的判空分支、inkmon 零调用的 `is_actor_dead`、两处复述 grant 契约的注释。⑦ 复审查漏的 5 条一并修掉：`_world_instance` 补显式拒绝写入的 setter（GDScript 只写 getter 时赋值仍会静默落进 backing 字段、把环接回来，而释放测试 case 2 走基类 procedure + `world.tick()` 收尾，结构上看不见这种环）；core 注释与 reference 文档里未经 helper 就解引用 `ctx.instance` 的示例改走 `world(ctx)`（`EventProcessor` 类头示例的 Post 观众同时改为效果前快照）；P4 C1 stdlib / example 清单的行号与计数刷新；释放测试文件头（`can_activate` 已被钉住）与 `entity.md`（`substr` 仍分配 String，只是不建 Dictionary）措辞更正。/ 有意不修（记后续观察）：context 存活计数机制、remove context 的 `event_processor` 恒 null、`.agents` 镜像无同步机制、dota2 冷却期空发激活请求、直接 `finish()` 后 `has_active_battle()` 陈旧；驳回：post 广播 / execution 的重复反查与 grant 投递开销（量级可忽略，缓存或递 instance 与「不经调用链递、不缓存」冲突）、HandlerContext 每次派发新建（P5 会改）、PreEvent filter 逐注册重建（可忽略）、㉑ 篇幅（只修两处措辞）。/ 为什么：① 是 P3 交付物「不成环证明」自身不成立，修代码比在证明里开例外干净，且改动只是三处字段声明；其余都是本阶段改动引入或暴露的口径漂移。/ 影响：dota2 lane 场景不再泄漏整场 LGF 图；hex / inkmon 行为不变（全量组 86 PASS）；core 单测 192 → 193。
- ㉓ P3 / 验收第 3 关第三轮复审 / 只审第二轮之后的增量（addons `1374c6b..HEAD` + 主仓对应文件）：十角度 → 去重 22 条 → 验证（T2–T4 交 agent，其余逐条对代码核实）→ 查漏 3 条，报 15 条，修复另起 addons commit：① procedure 回指 world 从「只读属性 + 拒写 setter」再改为子类协变覆盖 `_get_world()`——属性的 backing 字段恒为 null（调试器与回溯读的就是它），setter 守卫只在运行期、只守一个名字；方法没有存储槽，基类 `_world` 字段写明规则，dota2 `advance_tick` / `_advance_intents` 在函数头各取一次局部变量（`_eval_step_result` / `_remove_dead_actors` 仍逐次调，开销只是一次 `WeakRef.get_ref`；第四轮更正）；hex `smoke_mid_spawn_production_replay`（必跑组）加真实子类守卫：持住 `HexBattleProcedure`、放掉 world 后 world 必须已释放。② 收窄 CHANGELOG 与 ㉒① 的「中途 destroy 不再泄漏」：开着录像时 recorder 订阅闭包仍钉住它订阅的全部 actor（第四轮更正：不止参战者），不发信号的 abort 方案记后续观察并写进 P4 循环引用要点（连同 procedure 回边规则与三种释放用例形状）。③ 文档：`create_instance` 契约补进 CHANGELOG 迁移与 skill；`action-system.md`「固定值 vs 执行期计算」一节示例改真实 API（`HexBattleTargetSelectors` / `Resolvers.float_*` / `attribute_set.atk`；同页其余各节未动，见后续观察）；「instance 何时为 null」口径在 README §4 / `execution_context.gd` / `no_instance_component.gd` 对齐；README §4 / skill 写明 `world(ctx)` 只收 `ExecutionContext`、lifecycle context 的读点怎么写；`process_post_event` 写明观众快照规则；ActorId 注释改讲现状；释放测试文件头的盲区清单补全；`update.json` note 与 ⑳ 补上 `entity.md`。④ 小清理：hex `finish()` 删掉与基类重复的断连；damage_action 击杀判定直接用 `target_actor.is_dead()`，零调用的 `HexBattleGameStateUtils.is_actor_dead` 删除（其注释与实现相反的后续观察随之关闭）；`can_activate` 用例改用 builder。/ 有意不修：文档重复（README 与 skill 各自成篇是为了 skill 自足）、P4 C1 的 `file:line` 锚点整体改符号锚点（只改了 hex procedure 那一处；同轮漂移的 `battle_procedure.gd` flush 锚点第四轮补改，见 ㉔）、因果链在三处复述、`create_instance(factory)` 间接层（记后续观察，P4 C2）。/ 为什么：① 是第二轮修复自身的形状问题，② 是第二轮写下的过度声明，其余是同一增量里的口径漂移。/ 影响：行为不变；core 单测数不变（193）。
- ㉔ P3 / 验收第 3 关第四轮复审 / 只审第三轮增量（addons `d5107bd..7e987c1` + 主仓对应文件）：代码正确性 / 文档一致性 / altitude 三角度 → 去重 10 条 → 4 组 agent 验证（9 条成立、1 条驳回）→ 查漏（无新发现），修复另起 addons commit：① `SkillPreviewProcedure`（第四个 `BattleProcedure` 直接子类）补协变 `_get_world() -> HexWorldGameplayInstance`，删掉调用点的内联 `as`——它是唯一在调用点手工收窄的子类，`_fire_due_keyframes` 取到的基类型 world 还被隐式下转进 `HexFacing`；② 「procedure 子类只经协变 `_get_world()` 回指 world、不存 world 字段」写进 skill `entity.md` / `conventions-detail.md` 与 LGF README 设计铁律，`example-app-game-logic.md` 的 `tick_once` 范本补上 `var world := _get_world()` 与覆盖方法（原范本直接用未声明的 `world`，读着像字段）——㉒① 的三个子类是在「回指只用 id 或 WeakRef」这条通用铁律已经存在时照样犯的；③ P4「循环引用要点」的回边规则改正：原文要 controller / logger / spawner 也「只经 `_get_world()`」，可它们调不到这个 procedure 方法，照字面合规只能给 controller 加 procedure / world 字段、造出新环——改为「procedure 持有的对象不存 world / procedure，world 由 procedure 当调用参数传入」；④ 录像泄漏描述更正（后续观察、㉒①、㉓②、CHANGELOG）：钉住的是 recorder 订阅的全部 actor（开战时 `get_recordable_actors()` 全体 + 中途补录的 spawn；hex 中途召唤的图腾 / 火焰地形、inkmon 出战队之外的 roster 都在内，`ProjectileActor` 订阅为空不在内），拆环点除 `finish()` 外还有 hex demo / inkmon GI 的 `get_replay_data()`；P4 释放用例须含非参战的被录 actor；⑤ hex 伤害流程 5 处代码注释（`damage_action` / `reflect_damage_action` / `hex_battle_damage_utils` 头注释、harness、`smoke_world_view`）删掉「死后 remove_actor」，`damage_action` 击杀判定处补一句 why——第三轮改读 `target_actor.is_dead()` 的前提正是死者留在 world，注释不改会诱导把刚删掉的查表判定加回来；⑥ `action-system.md`「数值 / 坐标 / cue 参数一律是 Resolver」收窄（reflect / push / regenerate / spawn 系仍有裸值形参）；P4 C1 的 `battle_procedure.gd:129`（第三轮已漂到 131）与 `skill_preview_procedure.gd:228`（① 会让它漂）改符号锚点，P7 早已越界的 `ink_mon_battle_procedure.gd:148-165` 一并改成 `_create_action_use_event`；㉓ 两处措辞更正（dota2 只有 `advance_tick` / `_advance_intents` 在函数头取局部变量；锚点括注）；后续观察的假 API 清单补全。/ 驳回：「damage_action 击杀判定的语义变成回调跑完后才判死」——旧 `_check_target_killed` 同样排在回调之后、读的是同一个死亡锁存，`DamageResult.target_killed` 未改且全仓零读者。/ 有意不修（记后续观察）：`action-system.md` 其余各节与 LGF README 多节的假 API（要整页重写，与 target-selector 同批）；hex `damage-pipeline.md` / `shield-system.md` 顺序约束表的「remove_actor」（要改写约束的理由，不是换词）。/ 为什么：① ② ③ 是第三轮规则自身的覆盖面与表述缺口，④ ⑤ ⑥ 是第三轮写下或暴露的描述与现状不符。/ 影响：行为不变；全量组 86 PASS、直方图与基线一致；core 单测数不变（193）。
- ㉕ P3 / 验收第 3 关第五轮复审 / 只审第四轮增量（addons `7e987c1..4e8b46f` + 主仓第四轮改动）：代码注释 / 文档与计划两角度，报 3 条 + 1 条范围外附注，逐条对代码核实均成立，修复另起 addons commit：① `damage_action` on_kill 处第四轮新写的「死亡检测只锁存 `is_dead()`」去掉「只」——`apply_damage` 在回调之前已广播 death 并清 grid 占用；② harness 兜底循环的注释改为「当前走不到」：caster / ally / enemy 都取自 staging 的 `left_team` / `right_team`，上面的快照恒已覆盖，与死者是否留在 world 无关（第四轮把原因写错了）；③ 后续观察的假 API 清单补阶段依赖：`extends Action.BaseAction` 在 P6 删掉 `PrimitiveAction` / `FlowActionBase` / validator 之后即为正确写法，core 事件 key 在 P7 之前仍是 camelCase——不标的话，提前照清单重写会被 P6 / P7 打回；④ P9 的 harness 锚点 `:593-595` / `:646-647` 改符号锚点（已漂到 :589-591 / :639-640：P3 让该段上移 4 行，后者 P1 已先上移 3 行；第六轮更正）。/ 为什么：①② 是第四轮新写的注释自身不准，③ 是第四轮补全清单时没对照后续阶段，④ 是 P3（后者叠加 P1）造成的锚点漂移。/ 影响：只动注释与计划文字；第 1 关重跑 86 PASS、直方图与基线一致，core 单测数不变（193）。
- ㉖ P3 / 验收第 3 关第六轮复审 / 只审第五轮增量（addons `4e8b46f..0e54dfd` 两行注释 + 主仓第五轮计划改动）：两句注释核实无误；报计划文字 3 条，逐条对 commit 历史核实均成立，只改计划：① 假 API 清单的重写时机从「P6 / P7 之后」改为「P9 完成之后」——P8 把 `set_hp_base` 换成 `set_hp` / `add_hp`（README §5 的扣血示例按现状重写后会再次失效，而 P8 完成定义只 grep `.gd`），P9 让 grid 出 core；② P9「文档」条里 README 的 `:789` / `:317` 行锚点改为条目标题与目录树锚点（`:789` 经 P2、P3 漂到 :792，`:317` 在基线时就差一行、P3 后到 :308），并补上「新模型确立」段里 world 持有 `grid` / `configure_grid` 的描述；③ ㉕④ 的漂移行号与归因更正。/ 为什么：② 与 ㉕④ 同源——第五轮只刷新了 P9 改法第 4 条里的 harness 锚点，没核对同阶段「文档」条的 README 行锚点。/ 影响：只动计划文字；代码与第 1 关重跑（86 PASS、直方图与基线一致）时是同一状态，不再重跑。

- ㉗ P4 / 改法 C1「`ability_set.gd get_event_processor` 改从 instance 取；`_create_lifecycle_context` 与 `PreEventComponent._rebuild_context` 用手上已反查的 `owner_instance.event_processor`」/ 改为把 `AbilityLifecycleContext.event_processor` 做成从 `instance` 派生的只读属性（与 `ExecutionContext.event_collector` 同形），构造函数删 processor 参数（`instance` 成为第 5 个、最后一个），`AbilitySet.get_event_processor()` 随之零调用而删除。/ 为什么：采纳后续观察「`AbilityLifecycleContext` 三处手拼、字段来源不一」的修向——按计划写法三个构造点仍各自取值，`Ability._build_remove_context` 照旧传 null；派生后全部构造点（含 on_remove / 叠层 / Break）同源，processor 从已反查的 instance 读，「每次投递只反查一次」不受影响。/ 影响：`AbilityLifecycleContext.new` 的 3 处 core 与 17 处测试构造各少一个实参；on_remove 类钩子的 context 从此带 processor（今日无读者）。
- ㉘ P4 / 改法 C1「`_start_actor_action` / `_fire_due_keyframes` 里的 `face_actor_for_active_event`（`world.event_collector`）」/ 改为 `HexFacing.face_actor_for_active_event(actor, event, battle)` 删 collector 形参，函数在既有的 battle 判空之后取 `battle.event_collector`，harness 调用点同步。/ 为什么：两个 procedure 调用点的 `world` 来自 `_get_world()`、可为 null，照计划在实参里写 `world.event_collector` 会赶在函数自己的判空之前先解引用；battle 与 collector 同源，第二个形参本就冗余。/ 影响：hex 私有 helper 签名变化，3 个调用点。
- ㉙ P4 / 循环引用要点「开着录像的战斗中途 `shutdown()`……在「world 拥有战斗终止」落地前是预期泄漏」/ 本阶段落地了终止：`WorldGameplayInstance.end()` 在基类 despawn 之前对仍在进行的战斗调新增的 `BattleProcedure.abort()`（断开 `actor_added`、交还战斗槽位、新增的 `BattleRecorder.abort_recording()` 退订全部订阅闭包且不序列化、标记结束；不发 `battle_finished`、不清 in_combat、不经子类 `finish()`），释放测试用例 4 断言 recorder 与全部被录 actor 释放，而不是登记预期泄漏。首轮写在 `on_end()` 覆盖里，复审第 1 轮挪到 `end()`，并让 `finish()` / `abort()` 自己交还槽位、`tick()` 在战斗 tick 里 world 被结束时返回（见 ㉟）。/ 为什么：后续观察给的修向就是这一步；`shutdown()` 统一后中途拆除成为常规路径（inkmon `reset_session` / `load_game` 走 `destroy_all_instances`），只登记预期泄漏等于让硬关卡带着已知环过关。/ 影响：core 多 `abort()` / `abort_recording()` / `_release_battle()` / `_detach_from_world()` 四个方法与一个 `end()` 覆盖；对已结束的战斗 `abort()` 是 no-op。
- ㉚ P4 / 后续观察「context 存活没有机制级守卫」（建议本阶段落地）/ 两类 context 加存活计数（`static var` 在 `_init` 加一、`NOTIFICATION_PREDELETE` 减一）与 `get_live_count()`；释放测试四个用例各断言计数回到用例前，hex `smoke_skill_scenarios` 与 inkmon `smoke_m1_battle` 收尾断言归零。观察原文要 `OS.is_debug_build()` 门控，首轮照做（`get_debug_live_count()`），复审第 1 轮去掉门控并改名（见 ㉟）。/ 为什么：weakref 探针只覆盖四条构造路径，计数覆盖全部构造点；引擎每次释放都向脚本 `_notification` 分派 PREDELETE 与 PREDELETE_CLEANUP 两次、与构建类型无关，门控省不掉分派，只会多一次 `OS.is_debug_build()` 调用，并让 release 下的归零断言恒真。/ 影响：两个 smoke 各多一条断言；每个 context 多一次静态自增与两次 `_notification` 分派。
- ㉛ P4 / 后续观察「`GameWorld.create_instance(factory)` 的回调间接层已无用处」（挂 C2）/ 改为 `create_instance(instance) -> GameplayInstance`（注册并原样返回），调用点改成 `create_instance(X.new())`（原有的 `as X` 收窄保留）；5 处在 factory 体内配 grid 的 hex smoke 展平为「构造 → 配 grid → 注册」，其后恒不为 null 的判空块一并删除。/ 为什么：采纳该条修向。/ 影响：纯调用形态变化；撞 id 时仍是警告并返回已注册的实例（见后续观察）。
- ㉜ P4 / 改法 C1「`ExecutionContext.event_collector` 改派生只读属性（`get: …`，无存储字段）」/ 两个派生属性都加了拒绝写入的 setter（`Log.assert_crash`）。/ 为什么：GDScript 属性一律带 backing 字段，只写 getter 时赋值会静默落进 backing 字段、getter 照旧读 instance，「只读」只是注释；setter 把误写变成响亮报错。backing 字段恒为空、调试器读到 null，已写进属性注释。/ 影响：无。
- ㉝ P4 / C1 / C2 的小处写法 / ① recorder 在 `_start_recorder()` 里构造（计划写在 `start()` 里构造再调它）：注入要 `_get_world()`，钩子里本就取 world，world 缺席时一并不建；② `record_current_frame_events()` 在 world 已释放时直接返回；③ `InkMonWorldGI._init` 传 `EventProcessorConfig.new(20)` 而非 `(20, 0)`：trace 走 `DEFAULT_TRACE_LEVEL`，不在常驻世界里再写死一份默认值（后续观察里「改默认值没覆盖到它们」正是这种写死）；④ dota2 `_remove_dead_actors` 在函数头取一次 `world` 局部变量，新读点 `world.event_collector` 与既有的 `get_actors` / `remove_actor` 共用。/ 影响：无行为变化。
- ㉞ P4 / 测试与文档超出计划清单 / 测试：`world_test` 加「shutdown 幂等」「instance 自持 processor / collector」，`event_processor_test` 加「pre handler 按 instance 隔离」，释放测试加用例 3（procedure 子类形状 `ProbeWorld` / `ProbeProcedure` + 只经参数拿 world 的 helper，调用方直接 `finish()`）与用例 4（录像战斗中途 `shutdown()`，被录 actor 含非参战常驻者与中途 spawn）；`active_use_query_test` 夹具改为把 owner 注册进 instance（激活失败事件现在推 owner 所属 instance 的 collector），`no_instance_component_test` 读 instance 的 collector。「真实 procedure 子类」在 core 用测试子类覆盖形状（core 测试不依赖 example），生产子类的 world 释放守卫仍是 P3 加在 hex `smoke_mid_spawn_production_replay` 里的那条。core 单测 193 → 198，释放测试 4 例依次 41 / 69 / 47 / 82 条断言。文档：计划「文档」条之外还改了 skill `reference/{abilities,actions,stdlib}.md` 与 `SKILL.md §4`、LGF README 设计铁律「子对象回指 container」「测试引擎按场景独立」两条、hex frontend README、主仓 `docs/main-game-architecture.md` 开机句。/ 为什么：计划测试条只点名三个文件，而 shutdown 幂等、handler 隔离、中途拆除都是本阶段的新合同；文档条之外的几处引用了本阶段删除或改签名的 API。/ 影响：见数字。
- ㉟ P4 / 验收第 3 关 / `/code-review max`（addons `0e54dfd..0b6291e` 路径目标，finder 同时读主仓工作区全部 P4 改动）十角度 → 去重 28 条 → 分组验证（22 条成立或存疑、6 条驳回）→ 查漏 2 条，报 15 条，修复另起 addons commit：① world 结束时的中止从 `on_end()` 覆盖挪到 `end()` 覆盖（`on_end` 是基类留给子类的空钩子，子类覆盖漏调 super 就把泄漏接回来，而释放测试用的是基类 world、看不见）；`finish()` / `abort()` 经新增的 `world._release_battle(self)` 交还战斗槽位（槽位仍指着它才清），断连与交还收进 `_detach_from_world()`——后续观察「直接 `finish()` 后 `has_active_battle()` 陈旧」随之关闭，释放测试用例 3 原先把陈旧指针当预期断言，改为断言槽位已交还；`tick()` 用局部变量持 procedure，战斗 tick 里 world 被结束时本帧返回（首轮 `end()` 清槽位后，紧接着的 `_active_battle.should_end()` 会对 null 调用）；inkmon `InkMonBattleProcedure.finish()` 的不录像分支原先自己清 in_combat 后直接返回、不经 `super.finish()`，改为同样经基类收尾（否则漏掉交还槽位）。② 释放测试：用例 4 的中途 spawn 在 `add_actor` 之前挂 `probe_sink`（原先挂在之后，补录时 `setup_recording` 写进的是 actor 默认的空字典，这条路径的 RecordingContext 从未进 weakref 清单）；用例 2–4 断言被录 actor 都已订阅且 RecordingContext 探针真捕到；查漏发现 world 结束已拆掉 world → procedure 这条边，「全部释放」看不见 procedure 强回指 world，用例 2–4 改为 world 结束后、仍持有 procedure 时放掉 world 局部引用并断言 world 当场释放；四个用例外壳收成 `_run_release_case.bind(builder)`，world 形共有的 weakref 收进 `_collect_world_refs`；文件头更正没挂探针的构造点（grant 时交给 `apply_effects` 的 lifecycle context 与 PreEvent 重建——trigger filter 探针捕的正是 AbilitySet 派发建的那个）。③ owner 未注册时的降级：`StageCueAction` / hex `SurgeTickAction` / dota2 `Dota2AttackStartedAction` 对 `ctx.event_collector` 判空（P4 前它恒为非空的 `GameWorld.event_collector`，现在给未注册 owner grant 带 cue 的 buff 会报脚本错误）；`instance_context_test` 新增用例覆盖三条降级（PreEvent 不注册 handler；激活被 Condition 拦下时跳过失败事件，去掉 tag 后同一请求能激活作对照；cue action 跳过推送并返回成功——本轮 PreEvent / ActiveUse 两条只查状态，守卫退化成报错也照过，复审第 2 轮补了日志断言，见 ㊱）；`action-architecture.md` lifecycle 一节不再说 context 带 `event_collector`。④ 注入面收窄：`ProjectileSystem` 删 collector 构造参数与零调用的 `set_event_collector()`，发事件时从 `get_instance()` 取（10 个构造点去参；原先漏传或传错世界会静默丢 hit / miss / pierce 事件）；`RecordingContext._init` 删与 recorder 重复的 collector 参数（新增 `BattleRecorder.get_event_collector()`）；删零调用、会对 null 解引用的 `ExecutionContext.push_event()`；`BattleRecorder._unsubscribe_all` 与 `unregister_actor` 的逐 actor 退订收进 `_release_subscription()`。⑤ 存活计数去掉 `OS.is_debug_build()` 门控并改名 `get_live_count()`（见 ㉚）；hex `smoke_skill_scenarios` 只有计数失败时，结果行不再写成「FAIL - N/N scenarios passed」。⑥ trace：新增 `EventProcessor.set_trace_level(level)`，递归深度超限的「无 trace」提示改指它（原提示「重跑时传 `EventProcessorConfig.new(max_depth, 1)`」对 `_init` 不收 config 的 hex / dota2 / skill-preview / harness 世界无从下手）；`event_processor.gd` 头注释的 config 示例改成可调的构造形态。⑦ 文档与注释：CHANGELOG 补 Fixed 条目（world 结束中止战斗 / 槽位交还）、给 P3 Fixed 条目的「不在本条范围」加后向注，Changed / Removed / Added 同步本轮 API；过期注释（`Actor.gd` 的「EventProcessor 全局表」、harness / `raw_attribute_set` / `scenario_assert_context` 的「destroy 前」、`skill_preview_world` 的「关闭时 destroy」）；skill `entity` / `stdlib` / `actions` / `abilities` / `events` / `conventions-detail` 与 LGF README (c) 同步；P5 改法里 `rebuild_for_handler` 删 `event_processor` 形参（㉗ 之后 context 从 instance 派生 processor）。⑧ 小清理：查漏发现 `world_test` 的 shutdown 幂等只在已清空的注册表上复调、没覆盖「已 `end()` 仍在册」，把原先的 running 实例换成手动 `end()` 过的实例，断言 shutdown 对它与未启动的实例都只结束一次（running 形状因此丢失，第 2 轮补回，见 ㊱）；inkmon host / `smoke_m1_battle` / `smoke_battle_golden` 删 `create_instance(X.new()) as X` 之后恒不为 null 的判空，`ink_mon_battle_damage_utils` 删 `battle` 判空分支（唯一调用方 `InkMonDamageAction` 在 battle 为 null 时拿不到 target、先 `continue`，进来的 battle 恒非空）。/ 有意不修：`GameplayInstance.event_processor` / `event_collector` 仍是可赋值字段——recorder / RecordingContext / PreEvent 注销闭包都在构造时取走引用，中途换新会把生产者与消费者拆开，但全仓没有写入者，改只读属性要给每次读加一次 getter 调用；不经 `start_battle` 直接 `start()` 的 procedure 不占槽位、world 结束时不会被中止——全仓只有两个 hex smoke 这样用，`smoke_mid_spawn_production_replay` 自己 `finish()`、`smoke_knockback_punch` 不录像。/ 驳回：`event_processor_test` 的 handler 隔离用例「只证明两个 processor 不同」（隔离就来自每个 instance 各持一个 processor；grant 注册到 owner 所属 processor 由 `instance_context_test` 的 pre handler 用例钉住）；派生属性改方法（计划 C1 定的是派生只读属性、37 处读点零改动；P3 ㉓ 换成方法是为了去掉会接回强引用环的 world 存储槽，这里写入不成环、setter 已拒写）；CHANGELOG P3 条目的「随后…」后向注（沿用既有惯例）；`ProbeProcedure.tick_once` 不调 super（hex / skill-preview / inkmon 三个生产子类的 `tick_once` 同样整体覆盖、不调 super，测试子类照同形）；`instance_context_test` 经 `get_instance_of_actor` 取 processor（期望值取自独立的 `actor.get_gameplay_instance_id()`，反查若错 handler 不会触发、断言照样失败）；`ctx.event_collector` 每次读是一次 getter 调用（量级可忽略）。/ 为什么：① 是首轮 abort 方案自身的形状问题，外加一处既有的陈旧状态；② ③ 是首轮测试与降级路径的盲区；其余是本阶段改动引入或暴露的冗余与口径漂移。/ 影响：`ProjectileSystem.new` / `RecordingContext.new` 签名变化（调用点已全改）；行为变化：owner 未注册时的降级、world 中途结束时中止战斗、`finish()` 之后槽位即交还（dota2 这类直接 `finish()` 的流程之后 `has_active_battle()` 为 false、同一 world 可再开战）、release 构建的存活计数从恒 0 变为真实计数；core 单测 198 → 199，释放测试 4 例依次 41 / 74 / 52 / 88 条断言；全量组 86 PASS、日志零 SCRIPT ERROR、直方图与基线一致。
- ㊱ P4 / 验收第 3 关复审 / `/code-review max` 复审第 1 轮修复（addons `0b6291e..70959aa` + 主仓对应改动）：六角度 → 去重 16 组 → 逐条对代码核实（15 组成立、1 组存疑；「运行期报错只中止出错那一帧」与 `Logger` 的捕获形态另用独立 Godot 4.7 探针工程实测）→ 查漏无新条目，并复核本轮拟定修法（采纳：槽位保留兜底改为先断言、`world.tick()` 外也挂计数器、只数特定文本的警告），报 14 条，修复另起 addons commit：① 战斗槽位：`WorldGameplayInstance.tick()`（`finish()` 之后）与 `end()`（`abort()` 之后）先 `Log.assert_crash` 槽位已被 procedure 交还，再兜底清空——原先无条件清空，子类 `finish()` 漏调 `super.finish()` 时照样被清掉（inkmon `smoke_m1_battle` 断言槽位已空、照过），开着录像时随后的 `end()` 还会因槽位已空跳过中止；只删不留兜底则坏子类每 tick 重跑 `finish()`、重发 `battle_finished`（inkmon host 每帧重跑 `_on_battle_finished`），所以保留兜底、以断言报错；`tick()` 注释写明提前返回也覆盖 procedure 在自己 tick 里 `finish()` 的情形、那次 `tick_once()` 余下的函数体照样跑完；hex demo GI / skill preview 两处注释改指 `finish()` / `_detach_from_world`。② 新增测试辅助 `tests/log_counter.gd`（`extends Logger`，preload 引用、不注册 class_name，计数在锁内）：GDScript 运行期错误与断言失败只中止出错那一帧、调用方拿到默认返回值——PreEvent / ActiveUse 的守卫退化成报错时状态与正常降级相同，只能从日志通道分辨（cue 报错时 `execute` 返回 null，结果断言另能抓到）。`instance_context_test` 降级用例全程挂它，断言零错误、恰好一条「EventProcessor not available」警告（第 1 轮版本删掉 PreEvent / ActiveUse 的判空仍全绿）。③ 释放测试用例 4 改为经 `world.tick()` 驱动、在 `ProbeProcedure` 的 tick 里先 `mark_finished()` 再 `GameWorld.shutdown()`，world 用覆盖 `on_end()` 且不调 super 的 `ProbeWorld`，tick 期间挂计数器：第 1 轮加的提前返回与「中止写在 `end()` 不写在 `on_end()`」原先都没有用例能验（旧用例 4 从不调 `world.tick()`，「不发 `battle_finished`」恒真）；现在删掉提前返回会走 `finish()` 发信号，改回 `_active_battle.should_end()` 会报 null 调用，中止挪回 `on_end()` 会走 `finish()` 发信号——三种回退都以断言失败暴露，而不是 INT_MAX 帧空转。④ `world_test` 的 shutdown 用例补回 running 实例（running / 手动 end / 未启动各一个，都只结束一次），删掉第二次 shutdown 后恒真的 `end_calls` 断言。⑤ 文档与注释：CHANGELOG 的 `ProjectileSystem._init` 形参名（`auto_remove_val`）、P3 条目「6 用例」后向注、「全量组日志零此类警告」改为「只有降级用例刻意触发的那一条」、Fixed 条目的槽位与提前返回措辞；skill `entity.md` 的 `end()` / `tick()` / `_get_world()` 三条与 `example-app-game-logic.md` 样例注释（强回指泄漏的触发条件从「战斗不经 `world.tick()` 结束」改为「绕过 `finish()` / `abort()` 的任何退出」）；`recording_context.gd` 头注释与字段注释改讲现状；`smoke_skill_scenarios` 文件头补结果行与退出码；㉙ 方法数、㉟ 的 ③ ⑧ 与「影响」（直接 `finish()` 也改了行为）。/ 有意不修：hex `SurgeTickAction` / dota2 `Dota2AttackStartedAction` 的判空不补用例（与 `StageCueAction` 同形三行，core 用例钉住的是这个形状）；日志计数器只用在两个 P4 用例里，测试入口不因 SCRIPT ERROR 判负记后续观察。/ 记后续观察：`finish()` 清 in_combat 的 `TagChanged` 漏进下一场录像首帧（P4 之前即如此）；战斗 tick 内拆世界时正在执行的 `tick_once()` 仍跑完函数体；测试入口不因 SCRIPT ERROR 判负。/ 为什么：① 是第 1 轮修复留下的双写；② ③ 是第 1 轮新增测试钉不住它们声称钉住的东西；④ ⑤ 是第 1 轮文字与现状的出入。/ 影响：经 `world.tick()` 收尾或 `end()` 中止时，procedure 没交还槽位从静默变为报错（release 构建 `OS.crash`），调用方直接 `finish()` 的路径没有这道检查；core 单测数不变（199），释放测试 4 例依次 41 / 74 / 52 / 91 条断言；全量组 86 PASS、日志零 SCRIPT ERROR、直方图与基线一致。
- ㊲ P4 / 验收第 3 关复审第 3 轮 / 只审第 2 轮增量（addons `70959aa..47eeb8c` + 主仓对应改动）：代码与测试 / 文档与计划 / altitude 与调用方三角度 → 6 条，逐条对代码与提交历史核实均成立，修复另起 addons commit：① 释放测试用例 4 分不出中止与收尾——把 `end()` 里的 `abort()` 换成 `finish()` 照样全绿（`ProbeProcedure` 不覆盖 `finish()`，`stop_recording` 不发信号）；`ProbeProcedure` 加 `finish()` 调用计数，用例 3 断言 1、用例 4 断言 0。② CHANGELOG 与 ㊱ 的「漏调 super 会响亮报错」限定到 `world.tick()` 收尾与 `end()` 中止：调用方直接 `finish()` 的路径不经过槽位断言（基类查不出覆盖有没有调 super）。③ 降级措辞：`StageCueAction` 属 stdlib；「报错后状态与正常降级相同、只有日志分得开」只对 PreEvent / ActiveUse 两条成立，cue 报错时 `execute` 返回 null。④ ㉟⑧ 的 `world_test` 历史更正：第 1 轮前的用例注册了 running 与未启动两个实例，只是复调 shutdown 时注册表已空；第 1 轮把 running 换成了手动 `end()` 的实例。⑤ 注释：hex demo GI 的 `_hex_procedure` 是强类型访问句柄而非延命（`tick()` 的局部变量已把 procedure 持过 emit）；`RecordingContext` 的 `is_recording` guard 拦的是停录后仍残留的回调（`_unsubscribe_all` 先退订再清标志，不存在「stop 与退订之间」的窗口）。/ 为什么：① 是第 2 轮重写用例 4 时留下的盲区，② – ⑤ 是第 2 轮文字与现状的出入。/ 影响：只动测试与文字；core 单测数不变（199），释放测试 4 例依次 41 / 74 / 53 / 92 条断言；全量组 86 PASS、日志零 SCRIPT ERROR、直方图与基线一致。
- ㊳ P4 / 验收第 3 关复审第 4 轮 / 只审第 3 轮增量（addons `47eeb8c..a60053b` + 主仓对应改动）：代码与测试 / 文字两角度，代码零发现，文字 2 条均为非实质的措辞出入，改后不再复审：① ㊱② 的「报错后状态与正常降级相同、只能从日志分辨」补上只对 PreEvent / ActiveUse 成立（与 ㊲③ 对齐）；② 释放测试 `finish_calls` 注释只举 hex 在 `finish()` 里存战斗日志（写回放、改任务状态在 `battle_finished` 的 handler 里）。/ 影响：只动注释与计划文字，addons 另起 commit；重跑必跑组 16 PASS（core 199/199），全量组与直方图沿用第 3 轮修复后的重跑（其后代码只改了这一行注释）。

- ㊴ P5 / 改法 1「`HandlerContext` 加 `event_processor`（派发时设置）」/ 没加；`HandlerContext` 改由注册（`PreHandlerRegistration` / `PostHandlerRegistration`）构造时按 id 建好、每次派发复用，pre 派发同改。/ 为什么：㉗ 之后 `rebuild_for_handler` 不再收 processor，这个字段零读者；它还与复用冲突——注册缓存的 context 若带 processor，就是 processor → registration → context → processor 的环。P4 审查留给 P5 的「HandlerContext 每次派发都新建」一并收掉。/ 影响：`PreHandlerRegistration.call_handler(mutable)` 去掉 ctx 形参（只有 processor 调用）；handler 跨派发拿到的是同一个 HandlerContext。
- ㊵ P5 / 改法 1「`remove_handlers_by_owner_id` 同时清两张表（后者并清 `_owner_seq`）」/ 清两张表照做，序号不在这里清：新增 `note_actor_removed(actor_id)`（`GameplayInstance.remove_actor` 调，注销该 owner 的 handler 与序号），`remove_handlers_by_owner_id` 只清 handler。/ 为什么：inkmon `reset_battle_runtime` 按 owner 清 handler 时 actor 仍在 registry；照计划清掉序号，它随后重新 grant 的 handler 会排到未登记 owner 的末位、脱离 registry 顺序。序号跟 registry 走，与 `note_actor_added` 对称。/ 影响：`event_processor_test` 的 owner 序号用例钉住两种清理的区别。
- ㊶ P5 / 改法 3、4「`Dota2BattleActor` / `InkMonBattleActor.is_event_responsive` = `not _is_dead`」/ 两家不加覆盖，`BattleActor.is_event_responsive` 的默认就是 `not _is_dead`，只有 hex 覆盖。/ 为什么：计划这两条写于 P2 之前；P2 已把三家逐字相同的 `is_pre_event_responsive` 下沉为 `BattleActor` 默认（README 铁律同述），照写就是两份副本。/ 影响：行为与计划一致。
- ㊷ P5 / 改法 1「旧三参 `process_post_event` 保留为忽略观众的 shim，改法 5 再删」/ 不留 shim，同一提交内把全部调用点改成单参。/ 为什么：shim 只为分步提交时仍能编译，本阶段一次提交。/ 影响：无。
- ㊸ P5 / 「context 三处手拼，收成 `rebuild_for_handler` 时一并处理」（后续观察同条）/ 两个按 owner 反查的构造收进 `AbilityLifecycleContext`：`rebuild_for_handler`（pre / post handler，任一环节缺席返回 null）与 `for_ability(ability)`（on_remove / 叠层 / Break 钩子，永不为 null），共用 `_from_actor` 装配；`AbilitySet._create_lifecycle_context` 保留直接构造。/ 为什么：三处契约不同——set 自述（不经 registry，孤立单测里未注册 owner 的 grant 与定向投递靠它拿到 ability_set）、反查后必须照建、反查后可以放弃，合成一个工厂只能加模式参数；两个反查工厂共用装配后，两个 set 的来源只剩一处定义。/ 影响：`Ability._build_remove_context` 删除。
- ㊹ P5 / 改法 4 之外 / inkmon `InkMonWorldGI._reset_battle_state` 删掉逐单位 `remove_handlers_by_owner_id`。/ 为什么：roster 单位的注销已在 `reset_battle_runtime`（换集前），临时单位在 `remove_actor` 里，这一行重复。/ 影响：无。
- ㊺ P5 / 循环引用要点与测试超出计划清单 / ① pre / post 两种注册的注销闭包不捕获 registration（pre 原先捕获整个 registration；首轮审查后进一步改为只捕获注册表、kind 与 id，见 ㊾）：否则调用方仍握着 actor 时，component / ability 上的注销闭包在 `remove_actor` 之后仍钉住 registration 与它的 handler，计划要求的「remove_actor 后 registration 释放」不成立。② 释放测试另起用例 5（revoke / `remove_actor` / 整个换掉 AbilitySet 三种退场），不塞进前两例：换集那步专验 handler 闭包不钉旧 ability——`end()` 会清表，destroy 之后的断言看不见这种环。`_post_unregisters` 里的闭包不是 Object、取不到 weakref，由 ability 的 weakref 与「revoke 后已清空」断言兜住。③ 另加的测试：`event_processor_test` 的派发顺序 / owner 序号生命周期 / 注销三例；`instance_context_test` 的 post 派发 context 断言与「owner 未注册时不注册 post handler」降级断言（挂既有日志计数器）；hex 新 scenario `thorn_killing_blow_scenario`（致死一击的荆棘）。后两条做过回退验证：删掉 `Ability._register_post_handlers` 的判空，core 单测 211/212；把 hex 的 damage 豁免改成恒 false，scenario 68/69。④ `post_event_dispatch_test` 的嵌套派发用例刻意触发一条「Event recursion depth exceeded」错误日志（计数器断言恰一条）。/ 为什么：计划的测试清单只覆盖派发语义；本阶段新增的长期存放闭包要由释放测试逐条钉住，降级分支与致死一击两条路径没有别的测试会红。/ 影响：core 单测 199 → 212（审查修复再加 6 例，见 ㊾），hex scenario 68 → 69（计划写的「70 个」开工时实为 68），释放测试 5 例依次 44 / 80 / 59 / 104 / 34 条断言。
- ㊻ P5 / 行为变化核对（计划 ①–⑤、⑦ 成立；⑥「inkmon `reset_battle_runtime` 清幽灵注册」前提不成立：P5 之前 `_reset_battle_state` 已逐单位按 owner 清 handler，本阶段只是把注销挪进 `reset_battle_runtime`，见 ㊹）/ 另有两条计划没列，已写进 CHANGELOG：⑧ hex 死者对自己作为 target 的 damage 一律响应——D1 的豁免判不出「致死那一击」，而 `HexBattleProcedure` 照样 tick 尸体的 ability，尸体上残留的 DOT 结算时，尸体的荆棘会对 DOT 来源反伤（计划 ⑦ 只算了反伤打尸体）；⑨ `_process_abilities` 遍历快照后，某轮处理中新 grant 进本 set 的 ability 从下一轮起才被处理（此前遍历活数组，追加的元素本轮也会走到，本 actor 的 timeline action 自施的 buff，其周期 timeline 会在 grant 当帧推进）。全量组、hex scenario、`smoke_battle_golden` 指纹均不变。hex `view-logic-reconciliation.md` 里「Poison / Vitality 这类 tick 在死者身上停止」一句与 ⑧ 所述不符，随钩子改名删去。/ 为什么：D1 与改法 1 是固定决策，按原样落地，副作用记清楚；尸体该不该继续 tick 属范围外（见后续观察）。/ 影响：见上。
- ㊼ P5 / 小处写法 / ① `register_post_handler` 对定向投递 kind 同样断言（计划只要求 `process_post_event` 断言）；② post trace 逐 handler 的记录挂在 `trace["handlers"]`，`export_trace_log` 打印；③ 深度超限报错收成 `_depth_exceeded()`，pre / post 共用（原先两份逐字相同）；④ 两个 component 的 `get_post_event_kinds()` 共用静态 `AbilityComponent.trigger_event_kinds()`。/ 为什么：① 定向投递 kind 进了注册表永远派发不到（`process_post_event` 拒收这些 kind），是一条静默失效的订阅，在入口就报；② `intents` 是 pre 专用的意图记录，post 的逐 handler 记录形状不同；③④ 两份逐字相同的实现合成一份。/ 影响：无行为差异。
- ㊽ P5 / 文档与 skill / `/update-lgf-skill` 仍不可用（见 ⑤），手动增量：`enforcing-lgf/SKILL.md` §4 加「Post events are subscriptions」与检查清单一条，`reference/{events,entity,abilities,conventions-detail,example-app-game-logic,cast-eligibility-vs-condition}.md` 按本阶段 API 更新（events 加 `PostHandlerRegistration` 节，不写 `HandlerContext.event_processor`，见 ㊴），镜像到 `.agents/`（`diff -r` 逐字节一致），`update.json.last_commit` 记本阶段 addons SHA；主仓 `docs/future/architecture-optimization-plan.md` 的 R2「damage post-event 用 pre-damage alive list」标 ✅。/ 为什么：命令坏了，不在本阶段顺手修；R2 描述的问题随观众参数一起消失。/ 影响：同 ⑤。
- ㊾ P5 / `/code-review max` 首轮审查与复审修复，计划之外 / ① pre 派发同样遍历注册表快照（此前遍历活数组：handler 在派发中注销会让同 kind 的下一个 handler 被跳过）；注销闭包改由 `EventProcessor._make_unregister` 在 static 上下文里建，只捕获注册表、kind 与 id，不再捕获 processor（循环引用要点里「注销闭包捕获 processor 可接受」一句随之作废）；`_remove_handlers_where` 改为原地删除，不再逐次重建两张表。② `AbilityLifecycleContext.rebuild_for_handler` 对已过期的 ability 返回 null（派发快照里先一步过期的 ability 不再改 pre 事件）。③ `Ability.apply_effects` 在某个 component 的 `on_apply` 让本 ability 过期后立即停下：后面的 component 不再 apply，也不注册 post handler（首轮只挡住了 post 注册，复审补全）。④ post 注册与 handler 的 owner 取 context 的 owner（所在 AbilitySet），不取 `Ability.owner_actor_id`。⑤ hex `SkillPreviewWorldGI.reset()` 对被清掉的 actor 逐个 `note_actor_removed`。⑥ `PostHandlerRegistration` 不设 `seq`（改法 1 列了它）：同 owner 尾插即保持注册顺序，这个字段只写不读。⑦ `export_trace_log` 的 pre 分支 `as Array[Dictionary]` 运行期报错、导出中止（既有），改 `assign`。/ 为什么：①⑦ 是既有缺陷，P5 的快照语义与 post trace 让它们落在本阶段改动的面上；② 是 ① 的配套；③④ 不修会留下没人注销、按 owner 清不到的注册；⑤ 是常驻预览世界每次 RESET 累积注册与序号的现症；⑥ 只写不读的字段会让人以为顺序依赖它。/ 影响：新增测试——`event_processor_test` 的派发快照与 trace 导出、`post_event_dispatch_test` 的 ③④、`pre_event_component_test` 的 ②、`battle_actor_test` 的 `_process_abilities` 快照（并补死者不响应 post 的断言）、hex `shield_basic_absorb_scenario` 的「护盾当场移除」顺序、`smoke_skill_preview_reactive` 的 reset 后注册清零（caster 带荆棘，reset 前先断言有注册）、inkmon `smoke_session_spine` 的跨场注册数不累积；每条都做过回退验证（去掉对应修复即红）。core 单测 212 → 218。
- ㊿ P5 / 审查中确认、有意不修 / ① owner 未注册时 `Ability._register_post_handlers` 静默跳过（计划原文）：注册前 grant 的纯 post 被动永久缺订阅且无日志，登记后续观察；② post handler 触发的 action 里 expire 自己的 ability 不会被当场 revoke，登记后续观察；③ `PostHandlerRegistration.handler_context` 生产侧无读者、Pre / Post 两个注册类字段大半相同：两者保持同形，合并基类要连同 `filter` / `owner_seq` 的差异一起设计；④ `rebuild_for_handler` 取到 set 后 `_from_actor` 再调一次 `get_ability_set()`：开销可忽略，换来两个工厂只有一处装配；⑤ CHANGELOG / 注释 / 测试夹具里的重复表述不动；⑥ core 缺「整个换掉 AbilitySet」的出口、投射物 HIT / MISS 仍由 example 层回扫 collector 派发：范围外，登记后续观察；⑦ `Ability.owner_actor_id` 与所在 AbilitySet 的 owner 仍是两个来源（注册取后者，`AbilityLifecycleContext.for_ability` / `AbilityRef.from_ability` 取前者）：生产里两者恒相等，统一属范围外，登记后续观察（复审发现）。/ 为什么：①②⑦ 是计划之外的行为选择，要单独拍板；③–⑤ 不改行为，收益小于改动面。/ 影响：无。

## P5.5 文档清场

- P5.5-1 / 处置表「`docs/README.md`」/ 已知债务 ✅ 项当历史不搬，只留 grid-in-core 指向 D8 / P9；World owns Battle 压成三条；Action 原子性、框架层不收窄、dict / 强类型两端形态并入铁律；序列化 key 大小写不搬（P7 改）。/ 为什么：历史删、规则与 why 并入。[假设]
- P5.5-2 / 处置表「hex `docs/reference` 三篇」/ 捞入 hex README 铁律：伤害只走 `apply_damage` + 四条顺序约束、`damage_types` 取值、oracle 私有、死亡时 expire buff 须对称接 visualizer、事件定义约定。/ 为什么：顺序理由按「死亡锁存、不 remove_actor」改写。
- P5.5-3 / 处置表「addon `docs/reference`」/ 另捞入 SKILL.md §8：`super._init` 必调、目标经 `get_targets`、child 经 `execute_child`、`execution_state` key 带 namespace、跨时间响应走事件；按 P6 后合同措辞，横幅兜错位。/ 为什么：捞规则方法对每篇适用。
- P5.5-4 / 处置表「hex `README.md` 留」/ 删链接时顺手改正两处与代码矛盾的句子（「多数技能仍手抄门控四件套」「标准技能门控迁移」→ 21 个 active + preset 已全部经 helper）并把「见下未来规划」的债务句改成现状。/ 为什么：只讲现状；范围仍在同一文件。[假设]
- P5.5-5 / 处置表「后续观察搬已关闭」/ 除已标关闭 7 条外，P5.5 自身关闭 4 条（update-lgf-skill、.agents 镜像、参考文档教假 API、launcher 不判 SCRIPT ERROR）；dota2 CHANGELOG、根 AGENTS.md、17 处 .gd 注释不在表内未动，各记后续观察。/ 为什么：表外一律不动。[假设]
- P5.5-6 / chore 1 / 新分支在 `SMOKE_TEST_RESULT: FAIL` 后、`exitCode` 前，reason = 首条 `SCRIPT ERROR:` 行；先红后绿：`Log.assert_crash(false)` 注入 manifest lint smoke，旧 PASS → 新 FAIL → 还原 PASS。/ 为什么：按改法 1。
- P5.5-7 / 完成定义 grep / `docs/reference` 命中 `sim-nav-map*` 两个 skill 的 `addons/sim-nav-map/docs/references/0ad-source`（另一 addon 真实存在的目录），与 LGF 无关，视为零命中。/ 为什么：不在处置表，不动。[假设]
- P5.5-8 / 两问自查 / ① 本阶段无新表 / 注册 / 缓存（launcher 只多一条判定分支）；② 无新增或改动的循环。

## P6 Action/Config 收口

- P6-1 / 钉子 / hex `stance.gd` 是「先 `component_config` 后 `active_use`」的写法，按计划让 `AbilityConfigBuilder.active_use()` 插到普通 component 之前（`_active_use_count` 定位，active_use 之间保持调用顺序），钉子不重烤。/ 为什么：component 顺序 = 同 ability 内响应顺序，改成调用顺序会翻转 stance 的解析序。
- P6-2 / 改法 4 / `ActiveUseConfig._init` 形参 = 父类七参在前、`conditions` / `costs` 在后；`active_use_query_test` 收成一个 `_active_use()` 助手。`ActiveUseComponent._init` 不动，仍为默认 trigger 另建一份父配置。/ 为什么：计划只写「新构造顺序」，镜像层级最直接；组件层不在本阶段范围。[假设]
- P6-3 / 改法 1、4 清单差异 / `extends Action.PrimitiveAction` 实为 17 处（hex 多一处 `hex_facing._FaceTargetAction`）+ 1 处返回类型标注；builder 协变覆盖多一个 `on_cancel`（计划列 6 个）。/ 为什么：漏 `on_cancel` 会让链上 `.on_cancel(...)` 之后接不上 `.condition()`。
- P6-4 / 改法 4 / `SkillValidator`：`has_passive` = components 数 > active_use 数；components 循环跳过 `ActiveUseConfig`；`_check_structure` 判 `components.is_empty()`；lint `_collect_actions` 收成单循环。/ 为什么：`ActiveUseConfig` 现在也是 `ActivateInstanceConfig`，不跳过会把 active_use 的 actions 提两遍、主动技能误判带被动。
- P6-5 / 完成定义 grep / 计划正则 `TagAction\.` 也命中保留类 `LooseTagAction.`（32 处），按 `\bTagAction\.` 判零命中。/ 为什么：`LooseTagAction` 在 SKILL.md 指针表内，是现役公共原语。[假设]
- P6-6 / 测试 / 新行为测试的 `is ActivateInstanceConfig` 经 `Object` 变量查：对 `ActiveUseConfig` 变量直接写 `is` 在旧层级下是 analyzer 编译错误（parse error 不算红）。另补断言 timeline / tag_actions / conditions 原样落到 config。/ 为什么：§2 要求红的原因是断言。
- P6-7 / 源码注释 / `Action.gd` 头改两类表并指 SKILL.md §8；`execution_context.gd` 对 `docs/reference/action-architecture.md` 的引用改指规则之家；`loose_tag_action.gd` 头删「旧 TagAction」历史叙述；`hex_facing.gd` 注释去 PrimitiveAction 字样。/ 为什么：后续观察「.gd 注释指向已删文档」点名 P6 触及时改。
- P6-8 / 两问自查 / ① 无新表 / 注册 / 缓存：builder 的 `_active_use_count` 随 builder 销毁，`get_active_use_configs()` 每次新建数组；② 新增或改动的循环（`collect_timelines` / `get_active_use_configs` / `_resolve_components` / lint `_collect_actions`）遍历的都是 config 期后不再改动的 `components` 列表，无遍历中改集合。

## P7 key snake_case

- P7-1 / 改法 1 / 改名范围 = core+stdlib 全部 camelCase dict key（除计划清单外还有 registration / HandlerContext / trace / MutableEvent 记录 / 组件与 execution `serialize()` / TimelineData / ProjectileActor 配置与 launch_params / debug info，映射 116 条），另加 hex `shield_component` 的 `damageTypes` / `stackingPolicy`。/ 为什么：完成定义 grep 只认零命中，计划清单未列全。
- P7-2 / 改法 1 / 不动：action / condition / cost 的 `TYPE` 类型 id（`stageCue` / `launchProjectile` / `looseTagApply` / `hasTag` / `consumeTag` 等）、attribute config schema（`baseValue` / `minValue` / `maxValue` / `maxRef` / `minRef`）、前端 animation config key、`TestFramework` 内部 dict、hex meta `allowedTargetKinds`、`maxHp` 属性名兜底。/ 为什么：都不是事件 dict key / kind；schema 由 P8 沿用；改了不保今日行为。[假设]
- P7-3 / 改法 2 / 批量 = 两遍 `perl -i -p`：先带引号字面量（含 `\"…\"` 转义形，覆盖生成器模板），再 `\b` 词边界（注释 / 断言文案 / `launch_projectile_action` 两处 `event.targetPosition` 点访问）；11 份生成 attribute set 经 headless 生成器重生成（10 份有 diff，environment set 无 wrapper）。/ 为什么：点访问与转义模板是引号正则抓不到的读取点。
- P7-4 / 钉子 / golden 落盘走 `smoke_battle_golden.gd` 临时 env 分支，改前 JSON 递归套映射表（key + kind 值）与改后 JSON 逐字段相等、result / ticks / frames 全等后回填 `GOLDEN_HASH`；临时分支已删，脚本与两份 JSON 在 `.claude/tmp/p7/` 不入库；本阶段无 submodule 钉子 commit。/ 为什么：计划把 P7 钉子只定义为这份比对。
- P7-5 / 改法 4 / casing 测试除 GameEvent / ProjectileEvents 外还钉 18 个 kind 常量、PlaybackData 五个内部类与 RawAttributeSet 监听 dict（共 5 例）；hex `smoke_manifest_lint` 的可选 BattleEvents 断言未加。/ 为什么：D5 点名的四家都要机器守门；BattleEvents 本就 snake_case。[假设]
- P7-6 / 规则之家 / SKILL.md §4 加一条 bullet 并把 `abilityActivate` / `abilityGranted` 改新拼写；`scripts/CLAUDE.md` 加协议一段；hex frontend README 的录像 JSON 示例与两行 wire 说明同改（示例属性名 `maxHp` 改 hex 真实的 `max_hp`）。/ 为什么：README 示例是消费方会照抄的形状。
- P7-7 / 两问自查 / ① 无新增表 / 注册 / 缓存（只改 key 拼写；inkmon `_create_action_use_event` 改用栈作用域的 `GameEvent.AbilityActivate`）；② 无新增或改动的循环遍历被改集合（casing 测试只遍历本地新建 dict）。

## P8 资源型属性

- P8-1 / 改法 2、3 / 资源在生成 set 的 `apply_config` 字典里以 `"kind": "resource"` + `maxRef` 定义（内部走 `define_resource`），不另生成独立 `define_resource` 行；`max_ref` 允许晚于资源定义，写入 / 重 clamp 时才解析。/ 为什么：定义顺序 = 通知 / 快照 / 序列化顺序，hp 按字典序排在 max_hp 前才与今日一致，独立语句会把它排到末位。[假设]
- P8-2 / 改法 2「沿用 register_cross_attr_clamp 语义，改为资源专用实现」/ 通用跨属性 clamp 整条退役：`register_cross_attr_clamp` / `clear_cross_attr_clamps` / `_apply_cross_attr_clamps` / `_computing_set` 循环检测与 `BaseGeneratedAttributeSet` 包装都删；`maxRef` 只属于资源，stat 的 `maxRef`、任何 `minRef`、资源的 `maxValue` 生成器拒绝（`_validate_attr_kinds`）、`apply_config` assert。/ 为什么：唯一用户 hp ≤ max_hp 改走资源后它是零用户的双路径。[假设]
- P8-3 / 改法 2「add_modifier / update_modifier 指向资源 → assert_crash」/ assert 加在 `add_modifier` / `set_base` / `get_base` / `register_dynamic_dep`（源为资源）；`update_modifier` 不加。拒绝路径不写单测。/ 为什么：`update_modifier` 按 id 找 modifier，资源没有 modifier，路径不可达；`assert(false)` 打 SCRIPT ERROR，launcher 判 FAIL（同 `flow_action_test` 约定）。
- P8-4 / 测试 / 新行为测试分两次红：既有 API 能表达的（资源随 max_hp 下降不回升、serialize 形状、生成器产物与拒绝字段）按断言红；`set_resource` / `add_resource` / `define_resource` 用例在旧代码上是运行期 Nonexistent function（零断言判 FAIL）。/ 为什么：新方法不存在时无法按断言红。
- P8-5 / 改法 4 / hex / inkmon 扣血改 `add_hp(-x)`，其余 `set_hp`；inkmon `apply_derived_stats` 收尾的 `set_current_hp(minf(hp, max_hp))` 改 `sync_downed_state()`；`battle_actor_test` 的 `ProbeAttributeSet` 把 hp 改成资源，序列化断言改读 `value`；dota2 README 一句同改。/ 为什么：max_hp 重算时 attribute_set 已把 hp 拉低，剩下的只有 downed 对齐，行为相同。
- P8-6 / 改法 2 / `serialize` 资源形状 `{ "kind": "resource", "value" }`，`deserialize` 按 kind 还原成无上限资源（与 stat 一样不还原约束）。/ 为什么：计划只写「含资源值」，全仓无生产消费者。[假设]
- P8-7 / 两问自查 / ① 新增 `_attribute_names` / `_resource_values` / `_resource_max_refs` 都是 RawAttributeSet 私有表，随 set 释放，不涉及 revoke / remove_actor / reset / shutdown / 换 AbilitySet 五个出口；② `_reclamp_resources` 遍历 `keys()` 副本且只改既有键的值，`snapshot_current_values` / `serialize` 遍历定义期后不变的 `_attribute_names`，生成器循环遍历 config 快照。

## P9 grid 出 core

- P9-1 / 测试 / 计划外加 stdlib 单测 `tests/stdlib/grid/grid_world_gameplay_instance_test.gd`（4 例，登记 TEST_PATHS）；其红阶段是新类未声明的 TEST LOAD FAILURE 而非断言红。/ 为什么：hex 钉子覆盖不到 String occupant、无 `hex_position` 的 actor、null 三条 stdlib 合同；新类无法按断言红（同 P8-4）。[假设]
- P9-2 / 改法 4 / hex 死亡清占用从无守卫的 `grid.remove_occupant(pos)` 改为经 `clear_grid_footprint`「occupant 是自己才清」。/ 为什么：计划点名改调该方法；死者格 occupant 恒为自己（死亡不离格），hex scenario 69 例与 inkmon golden 不变，只是 overlay 死亡不再误清同格别人。
- P9-3 / 改法 4 / `world_view._get_world()` 返回类型随 `bind_world` 一起收窄为 `GridWorldGameplayInstance`。/ 为什么：`hex_to_world` / `_on_grid_configured` 读 `world.grid`，core 基类已无此字段。[假设]
- P9-4 / 改法 3、4 / 顺手改 5 处注释指针：`world_gameplay_instance.gd` / `world_view.gd` 的 `docs/README.md` → `CLAUDE.md`「World owns Battle」，`hex_world_gameplay_instance.gd` 的 `view-logic-reconciliation.md` → `tests/frontend/view_logic_reconciler.gd`，`piercing_line.gd` 与 hex `README.md` 的 `_clear_grid_footprint` → `clear_grid_footprint`。/ 为什么：P5.5 后续观察点名 P9 触及时改指规则之家；被删符号不留假引用。
- P9-5 / 钉子 / 钉子只钉 hex 层今日行为（新 smoke `smoke_grid_footprint` 进 hex/regression，钉子 commit `53aa1d4`），未钉「`hex_position` 无效时旧 `remove_actor` 不扫预订」的边角（新实现无条件按 id 扫）。/ 为什么：计划已明写新形状；仓内无 actor 在无效坐标下持预订的路径。[假设]
- P9-6 / 两问自查 / ① 唯一新表 = `GridWorldGameplayInstance.grid`（occupant / reservation）：`remove_actor` 经 `clear_grid_footprint` 清、inkmon `_reset_battle_state` 清 roster 足迹 + 移除临时单位、`SkillPreviewWorldGI.reset` 整板置 null、instance 结束随之释放（释放测试用例 2 加 grid weakref）；revoke / 换 AbilitySet 无关。② `clear_grid_footprint` 遍历 `get_all_coords()` 的新数组、只改 tile metadata；其余循环未新增。

## P10 整体审与收口

- P10-1 / 改法 2 / 未走 `/code-review max`：两份 diff 落盘 `.claude/tmp/p10/` 后开 3 个 fresh-context reviewer（① 退场对称与循环引用 ② P8 资源语义 ③ P9 grid）+ 1 个 verifier，全程 fable。/ 为什么：addons `origin/master` 已在 P5（78f8cc5）、主仓在 P5 状态行，review 的 diff 基线到不了 8e56bba / bbe5d9a7；`change_directory` 无人值守要人批。[假设]
- P10-2 / 改法 2 / 三角度共报 1 medium + 7 low + 若干范围外；medium（P8：max_hp 暂降回升后 hp 存值被吞）经 verifier 读旧代码 + HEAD 探针 + inkmon 真链路探针 CONFIRMED；low 与范围外只记 §6 后续观察，不修、不复审；四条已知设计题两条被再报，标「已知、待拍板」。/ 为什么：改法 2、5。
- P10-3 / 改法 3 / 修在 core：`RawAttributeSet` 资源读值按 maxRef 当前值封顶、不改存值（删 `_reclamp_resources` 与 8 处收尾调用，加 `_resource_current`；`serialize` 出封顶读值；`add_resource` 从读值起算），写入仍 clamp 到 `[min, maxRef 当前值]`；hex Break 撤销 / 恢复 Vigor 的同根因 low 一并消掉。/ 为什么：P8 写明「数值应完全一致」「沿用 register_cross_attr_clamp 语义」（旧语义 = 读时 clamp、存值不动），覆写存值是未登记的行为变化，取保住 P8 前行为。
- P10-4 / 改法 3 / P8 单测「上限回升不跟着回去」重烤为「回升后读值恢复」（事件 3→4 条，回升时 hp 事件在 max_hp 前）；新增 modifier 入口暂降用例（core 224→225）与 inkmon `smoke_actor_serialization` 带 max_hp 装备的 `apply_derived_stats` 幂等守卫（smoke 私有 `ItemCatalog` 注入 `item_9001`）。先红后绿：core 4 条断言红，inkmon 在改前 core 上「hp=138/188」红。/ 为什么：§2 第 1 关。
- P10-5 / 改法 3 / 与 P8 前唯一残差：显式写入高于当前上限的值时存值被截到上限（旧 `set_hp_base` 只按静态 min/max 截）；仓内 heal / regenerate / `set_current_hp` 都先按 max_hp 截再写，无现症。/ 为什么：D6「写入 clamp」字面保留。[假设]
- P10-6 / 改法 4 / 横幅只删「重构进行中」半句，保留「两个规则之家」一句；14 处 `.gd` 注释指针（`docs/README.md` / `view-logic-reconciliation.md` / `shield-system.md` / 「CHANGELOG 待处理 / D5」/ `modify_hp` / `maxHp`）改指规则之家或现役文件。/ 为什么：后续观察点名 P10 一并清。[假设]
- P10-7 / 第 1 关 / `-Required` 一次跑出 `smoke_skill_validator` exit=-1073741819（PASS 已打印、退出期 AV），单跑 3 次与整组重跑均 PASS，判定并行负载下的退出期偶发，记后续观察，不计修复轮。/ 为什么：该 smoke 不读资源属性，与 P10 改动无关。[假设]
- P10-8 / 两问自查 / ① 无新表 / 注册 / 缓存（`_resource_current` 纯读；smoke 私有 `_MaxHpGearCatalog` 随 `ItemSystem.reset_session` 退场）；② 无新增或改动的循环（删掉的重 clamp 循环本就遍历 `keys()` 副本）。

## P10 拍板落地（2026-09-13 拍板，2026-09-14 落地）

- P10.5-1 / 设计题 ① / `AbilitySet.grant_ability` 先 `get_owner_instance()`，为 null 即 `assert_crash` 并 return 不 grant；`ability_test` 排序用例与 `instance_context_test` 两条「未注册 owner」用例改为先 `add_actor`（后者改钉「instance 已销毁后定向投递仍送达、降级不报错」，「EventProcessor not available」由 1 条归零）；inkmon `smoke_actor_serialization` 两处装备用例用空 `GameplayInstance` 承载。拒绝路径不写单测（SCRIPT ERROR 判 FAIL 的约定）。/ 为什么：用户拍板——先登记再 grant 是流程铁律，静默容忍分支只为孤立单测而存在。直接调 `apply_effects` 的孤立单测仍走「没有 processor 不注册」，那个守卫保留。
- P10.5-2 / 设计题 ② / `Ability._make_post_handler` 调完 `receive_event` 后 `is_expired()` 即 `ability_set.revoke_ability(id, EXPIRED, expire_reason)`；`post_event_dispatch_test` +1（handler 内 expire 自己 → 同次派发除名、abilityRevoked 恰一次、注册清零，先红后绿）。hex 护盾破裂处的显式 revoke 保留——那是被伤害管线从外部打碎，不在自己的 handler 里 expire。/ 为什么：用户拍板「每条运行 ability 的路径自己回收」，与 tick / 定向投递经 `_process_abilities` 的清扫对称；不做成配置项（没有合理的关闭档位）。未采用「`expire()` 反查 set 自除名」：与 `revoke_ability` 先 expire 再按下标删的顺序重入。
- P10.5-3 / 设计题 ③ / `grant_ability` 盖章：`owner_actor_id` 为空即填 set 的 owner（`source_actor_id` 为空同步补齐），非空且不同即 `assert_crash` 不 grant；`_test_registration_owner_follows_ability_set` 扩为盖章断言（含显式 source 保留，先红后绿）。`Ability.new` 的 owner 形参保留，call site 不动。id 由工厂派发的更大重构列候选刀不排期（§6 后续观察新条目）。/ 为什么：用户拍板最小版；两个字段仍在，但只剩「故意传错」一种风险且当场断言。[假设] 空 owner 时 source 也补成 owner——沿用 `Ability._init` 的「source 缺省 = owner」语义。
- P10.5-4 / 设计题 ④ / 不改：Break 后 pre handler 仍改事件（hex 代达罗斯暴击穿透 Break）。/ 为什么：用户拍板不做，Break 技能未继续设计。
- P10.5-5 / 规则之家 / addon `CLAUDE.md` 加「grant 前先登记、owner 由 set 盖章、过期者由运行它的路径回收」一条、hp 条补「上限变化怎么跟是游戏层策略」；SKILL.md §2 / §4 / hp 段 / 检查清单同步。/ 为什么：§2 规则之家；max_hp 那句是用户拍板「表现由游戏决定，core 只给原语」。
- P10.5-6 / 第 1 关 / `all` 别名 127 scene 全 PASS（主链验收的 89 之超集，含 simnav / dota2lab 全组）；core 单测 225→226；日志零 SCRIPT ERROR、刻意基线只剩 recursion 一条；inkmon golden 指纹不变。四份泄漏直方图未重烤。/ 为什么：改动无新引用——post handler lambda 仍只捕获两个 id，revoke 走既有路径，grant 只多两个断言与两次字段赋值。[假设]

## 随机战斗差分与固化（2026-09-14）

- RD-1 / 方法 / 在独立 worktree 跑重构前基线（主仓 `bbe5d9a7` + addons `8e56bba`），headless 探针按 seed 跑 `HexRandomDemoWorldGameplayInstance`，逐 seed 比对 console.log（逐字节）、录像事件流（只归一化 key / kind 拼写、`recorded_at`、生成 id 序号化）与 loadout 摘要；比对器过突变测试（改数值 / 删事件 / 换序均红）。/ 为什么：用户要求确认战斗日志与行为同重构前一致。
- RD-2 / 结果 / demo 自带 20 个 seed：日志逐字节相同、事件流与胜负帧数全同。覆盖率组 30 个 seed（进攻位 1）：22 个全同，8 个首次分叉归四类——A 尸体荆棘反弹 DOT（P5 死者响应规则 × hex 尸体照样 tick，已登记）、B 过期清扫不再搭广播的同帧顺序（P5 行为变化 ①）、C max_hp 下降时立即发 hp 读值封顶通知（P8/P10；旧实现是懒缓存，hp 会滞留在上限之上直到下次写入）、D 遍历快照使 grant 当帧的周期 buff 不在同帧 tick、第二次 tick 落在精确周期第 20 帧而非第 19 帧。50 个 seed 胜负全同。
- RD-3 / 拍板 ① / hex `HexBattleProcedure.tick_once` 补 tick 循环跳过 `is_dead()`：尸体不 tick——DOT 不结算、周期 buff 不倒计时、荆棘不反弹；连带死前已开始的技能执行不再落地（差分里 900083 / 900113 / 900223 各有一记「死后余波」命中消失，胜负未变）。hex 28 scene 全 PASS。/ 为什么：用户 2026-09-14 拍板；skill-preview procedure 早已是「死者不 tick」，主战斗补齐。在飞执行随尸体一起冻结而非显式 cancel，用户 2026-09-14 确认按此保留。
- RD-4 / D 处置 / 未改代码：HEAD 的精确周期是对的，基线把 grant 那一帧重复计入；副作用是含周期 buff 的单位 ATB 时机最多偏 1 tick。记 §6 后续观察（含查证：同一 buff 时长与 timeline 在 grant 帧不同步、目标顺序依赖、`periodic` 定义与「loop 余量结转」都指向精确周期），golden 钉住 HEAD 行为。/ 为什么：P5 快照遍历顺手修掉了 initial commit 以来活数组遍历的偷跑，用户 2026-09-14 确认保留。
- RD-5 / 固化 / 新增 `tests/battle/smoke_random_battle_golden`（组 `hex/random-golden`，required）：10 个 seed（50 seed 差分 dump 上贪心 set cover，两个走 demo 原规则、八个走 `offensive_slots=1`），每场取归一化录像 + loadout 的 sha256 与 `random_battle_golden.json` 比对，并断言 manifest 全部 53 个 config 有证据（主动技能有 execution、随机池被动被装载、buff / 召唤物 / 内建有 execution / trigger / grant 任一；`buff_inspire` 录像前挂上，豁免）。重烤 `-- rebake`，须先解释。配套：`HexRandomDemoWorldGameplayInstance` 加 `offensive_slots` config 键；`HexDemoWorldGameplayInstance` 加 `save_replay` 键。/ 为什么：用户接受提议——技能逻辑执行顺序是两个月设计成果，要有长期守门。
- RD-6 / 覆盖率事实 / demo 3v3 时 `ceil(3×0.67)=3` 个进攻位让治疗 / 结界 / 两种护盾 / 涌动 / 姿态 / 图腾 / 净化 / 换位 9 个技能永不进 loadout，自带 20 seed 只覆盖 20/29 主动技能；`offensive_slots=1` 后 30 seed 全覆盖。/ 影响：`smoke_random_frontend_20_runs` 的 BUFF_SKILLS 分析分支实际从未执行过。

## 后续观察 B-core 小修（2026-09-17，followups 第 1 轮：B-1 / B-3 / B-4 / B-6；addons `09ac4ee`）

- FU1-1 / B-1 / `AbilitySet.revoke_abilities_where(predicate, reason) -> int` 落地，返回本调用亲手 revoke 的条数（命中快照里被前面某个 on_remove 连带退场的不计）；删 `revoke_abilities_by_config_id` / `revoke_abilities_by_ability_tag`，唯一调用点 `battle_actor_test` 改 predicate 写法。新增 `ability_set_revoke_test` 3 例（命中集合 / on_remove 级联走快照不跳过不二次退场 / reason 透传），先红（`:=` 推断新方法即 parse 红，与 P8「新 API 用例缺方法红」同形）后绿。inkmon `reset_battle_runtime` 换集前调它归 task-queue 2e。/ 为什么：用户拍板 B-1。[假设] 返回值取「亲手 revoke 数」而非命中数——级联退场的那条已广播过，再计一次会让调用方误以为多退了一条。
- FU1-2 / B-3 / 删 `GameWorld.destroy_all_instances`，`shutdown()` 合同放宽为「任何要干净注册表的时刻」；21 处调用机械改名（inkmon host 2 + inkmon smoke 18 + `world_test` 1），inkmon 只改动词。/ 为什么：函数体逐字相同、只差日志级别；inkmon smoke 日志多出的「GameWorld shutdown」info 行是唯一可见差异。
- FU1-3 / B-4 / 删 `Actor.serialize_base` / `BattleActor.serialize` / `GameplayInstance.serialize_base` / `ProjectileActor.serialize`、hex 三个与 inkmon 两个子类覆盖、`battle_actor_test` 两个 serialize 用例；全仓 `serialize_base` / `super.serialize()` 零残留。规则之家（addon `CLAUDE.md` 设计铁律）加「可序列化挂读者不挂类」；`enforcing-lgf/reference/conventions-detail.md` 的 BattleActor「现成」清单摘掉 `serialize()`。/ 为什么：TS 移植残留、生产零读者；ability / component / attribute 的 `serialize()` 有读者（录像 `AbilityGranted` payload），不在此列。
- FU1-4 / B-6 / `StatModifierComponent` / `DynamicStatModifierComponent` 的 on_remove / on_passive_disabled / on_passive_enabled 共用 `_raw_attribute_set_or_assert(context, hook)`：属性集为 null 即 `Log.assert_crash`「<hook>: owner 已不在 instance / instance 已销毁，modifier 无法清理」，仍清内部记录后返回；原 on_remove 裸解引用与四处静默守卫都改成这一条。新增 2 例（两个组件各一：销毁 instance 后 revoke → `matched_errors == 1 && errors == 1`、内部记录已清、退场走完），先红（null 解引用是引擎错误、不含断言文本）后绿。/ 为什么：用户拍板。[假设] `StatModifierComponent.on_stacks_changed` 的静默守卫未动——它不是清理 / 重建钩子、B-6 未点名，null 时只是不更新、不泄漏；Dynamic 的 `_modifier_id` 本就留给 enabled 重用，on_remove 不清。
- FU1-5 / 测试设施 / 断言路径可以钉了：`tests/log_counter.gd` 加 `matched_errors`（同一 match_text 同时数 error 与 warning）；`TestFramework.expect_script_errors(n)` 累计后在汇总末尾打印 `EXPECTED_SCRIPT_ERRORS: n`，主仓 `tools/run_tests.ps1` 的 `Finish-Scene` 改为「日志里 `SCRIPT ERROR:` 条数 == 声明数才放行」——多一条（同场景别的用例出错）或少一条（断言没触发）都 FAIL，没声明的场景仍任何一条即 FAIL。/ 为什么：B-6 要求用 log_counter 钉「恰好一条断言」，而断言必打 `SCRIPT ERROR:`、launcher 原规则一条即 FAIL（P10.5-1 因此未钉 grant 拒绝路径）；`OS.add_logger` 的计数器与 stderr 同受 `print_error_messages` 门控，关掉输出会连计数一起关掉，只能让 launcher 认声明数。[假设] 取「恰好 n 条」而非「≤ n」。
- FU1-6 / 第 1 关 / `core/unit` 全绿（+3 revoke_where、+2 orphan revoke、−1 serialize；释放测试随组绿）；`all` 别名 128 scene 127 PASS，唯一 FAIL `hex/random-golden` 5 seed 指纹漂移（胜负 / 帧数 / 事件数全同、仅 sha）——隔离 worktree（主仓 `93caa156` + addons `7182e8f` + 仅本轮 12 文件 patch）复跑 10 seed 全部不变 PASS，漂移来自同一工作区第 2 轮 B-2「投射物产出即派发」的未提交改动（B-2 拍板即预期漂移），本轮不重烤；inkmon `smoke_battle_golden` PASS 指纹不变；四份泄漏直方图未重烤。/ 为什么：本轮无新持引用（predicate 与快照只在调用栈上、helper 不缓存 context）。[假设]
- FU1-7 / 两问自查 / ① 新增表 / 注册 / 缓存：无（`revoke_abilities_where` 的快照是局部变量；`_expected_script_errors` 是测试框架计数、`run()` 开头清零）。② 新增或改动的循环：`revoke_abilities_where` 遍历 `to_revoke` 快照；两个组件的钩子与改名不涉及循环。
- FU1-8 / 范围外新发现（记 §6，未修）/ `AbilitySet.revoke_ability` 先按下标定位、再 `expire()`、最后 `remove_at(index)`：on_remove 里 revoke 了排在自己前面的兄弟时下标已左移，`remove_at` 删错人或越界。今天无生产 on_remove 会 revoke 兄弟；级联用例特意让目标排在级联者之后。

## 后续观察 B-hex（2026-09-17，followups 第 2 轮：B-2 / B-7 / B-5 / B-10；addons `a66d3d9`）

- FU2-1 / B-2 / `ProjectileSystem._emit_hit/miss/pierce` 推所属 instance 的 `event_collector` 后当场 `process_post_event`（despawn 只录不派发；system 未注册 instance 即短路）；删 `HexWorldGameplayInstance.broadcast_projectile_events` 与 hex procedure / skill-preview procedure / harness 三个回扫调用点（harness 的 `tick` 覆盖整个删掉，继承 `WorldGameplayInstance.tick`）。/ 为什么：用户拍板 B-2；投射物维持 core `Actor` 载体。[假设] PIERCE 从此也派发（原回扫只发 HIT / MISS）——三者同一产出点，hex 今日无 pierce 订阅者。
- FU2-2 / B-2 测试 / 新增 stdlib 单测 `tests/stdlib/projectile/projectile_system_test`（4 例：hit / miss / pierce 的 collector 顺序 = 事件 → 订阅者回声 → despawn、回声里弹体仍在注册表且 tick 末被 auto_remove、未注册 system `force_hit` 不派发不报错），登记 `TEST_PATHS`；先红（3 例按断言红：回声缺席）后绿。并行开发期间用 `.claude/tmp/b2/` 私有 runner 跑，第 1 轮 commit 进 log 后才把登记行加回共享的 `run_tests.gd`。/ 为什么：§2 第 1 关。
- FU2-3 / B-2 golden / 改码前后各 dump 10 seed 原始录像逐帧比对（探针 + 差分脚本在 `.claude/tmp/b2/`，不入库）：5 seed 逐字节相同，5 seed（571031 / 671159 / 900097 / 900127 / 900131）共 13 帧只差 `projectile_despawn` 相对命中反应事件的位置——命中当刻结算，hit 触发的反应落在 hit 与 despawn 之间；胜负 / 帧数 / 事件数 / loadout summary 全同。解释后 `-- rebake`，5 个 sha 更新。/ 为什么：§2「golden 漂移先解释再重烤」。
- FU2-4 / B-7 逻辑 / `AIStrategy._move_toward / _move_away_from / _is_tile_available` 加 `battle` 参数读 `battle.grid`（melee / ranged / support / random_loadout 含两个计数 helper 同步）；`HexBattleAngleCone` 三个 static 几何函数首参 `grid: GridMapModel`，stage-cue params resolver 经 `HexBattleGameStateUtils.world(ctx).grid`、selector 经 `battle.grid`；`skill_preview` 10 处改 `_world.grid`（含 `coord_to_world` / `world_to_coord`）；harness 放人走 `self.grid`；删 hex `configure_grid` 覆盖。/ 为什么：用户拍板 B-7。[假设] resolver / selector 在 `grid == null` 时返回空，与它们对 caster 缺席的既有形状一致，不断言。
- FU2-5 / B-7 前端 / `FrontendFacingIndicatorView` 改由注入的 `GridLayout` 算方向（无布局退化固定 60°，裸建 UnitView 的 smoke 形状不变）：`FrontendUnitView.set_grid_layout` 存字段并转发（`_ready` 前注入也保留）；`WorldView` 在 `_spawn_unit_view` 与 `grid_configured` 时灌 `world.grid.get_layout()`；`BattleAnimator` 对自建的录像 view 灌 `director.get_grid_layout()`（新增 `FrontendBattleDirector.get_grid_layout` → `FrontendRenderWorld.get_grid_layout`，即录像 map_config 建的 layout）。/ 为什么：「从其渲染的 world 取」= 创建 view 的一方注入，view 不反查 world。[假设] 注入 `GridLayout` 而非整张 `GridMapModel`——箭头只要几何，录像播放侧本就只有 layout。
- FU2-6 / B-7 插件与文档 / `ultra-grid-map/plugin.gd` 去掉 autoload 自动注册（只剩说明注释），`project.godot` 删 `UGridMap` 行；`u_grid_map.gd` 保留为「可选、不自动注册」的单例脚本，README 相应改口；主仓 `CLAUDE.md` / `AGENTS.md` / `docs/project-overview.md` autoload 列表、`game-architecture-patterns` skill 两处、`example/README.md`、stdlib `configure_grid` 注释同步。/ 为什么：计划只写「去掉自动注册」，删脚本属扩大范围，记 §6。[假设]
- FU2-7 / B-7 测试 / `smoke_grid_footprint` 删「grid == UGridMap.model」断言，新增「另一个 world 配 radius 1 棋盘后，本 world（radius 3）的战士 AI 仍走 (2, 0)」——先红（旧代码读全局槽位得 `skip`）后绿；`smoke_summon_spike` 五个阶段改走 `instance.configure_grid` + `instance.grid`，输出标记改 `SMOKE_TEST_RESULT`，登记进 `hex/skills`（5/5 PASS，placeholder 1 skip）。B-7 前后 10 seed 事件流逐字节相同（同板不漂）。/ 为什么：B-7 验收要点。[假设] 登记进 `hex/skills` 而非 required 的 `regression`——它是原语探针不是回归面。
- FU2-8 / B-5 / `item_preview._setup_session` 经 `GameWorld.create_instance` 登记沙盒，`_clear_sandbox_world`（reset 与 `_exit_tree` 共用）先 `destroy_instance`；`smoke_item_preview_boot` 加三段断言（actor 可经 `GameWorld.get_actor` 反查 / `reset_sandbox` 后旧 instance 注销、新 instance 可反查 / `queue_free` 后无残留），先红（反查为 null）后绿。不加种子。/ 为什么：用户拍板 B-5。
- FU2-9 / B-10 / `Dota2AutoBattleProcedure._request_basic_attack` 发请求前 `AbilityActivationQuery.is_allowed(ability_set.can_activate(ability, event))`，不通过即 return（5d 循环末尾唯一调用点，等价于 continue）；`smoke_lane_wave_engage` 统计 `ability_activate_failed` 断言为零，先红（1220）后绿；日志「条件不满足」1220→0、「开始执行」85→85（攻击时机不变），dota2 frontend smoke 同样 0。/ 为什么：用户拍板 B-10。[假设] 预检写在 helper 内而非循环体，`continue` 语义不变。
- FU2-10 / 规则之家 / addon `CLAUDE.md` 设计铁律加「实体 vs 载体」（§3 措辞 + `ProjectileSystem` 产出即派发的落点），「core 无 grid」条末补「一条棋盘真相：棋盘只归 world 持有、由参数递入，没有全局槽位」。/ 为什么：§2 规则之家只改规则变了的行。
- FU2-11 / 第 1 关 / `-Required` 18 scene 全 PASS（core 单测 +4，释放测试随组绿）；`all` 别名 129 scene 全 PASS；golden 重烤后 `hex/random-golden` PASS；无钉子 commit（本轮钉子即 golden）；四份泄漏直方图未重烤。/ 为什么：改动无新持引用——`ProjectileSystem` 仍只经 `get_instance()` 弱引用取 instance；`GridLayout` 是 view 持的几何值对象，不指回 world / actor；沙盒 instance 由 `GameWorld` 持、两个出口都 `destroy_instance`。[假设]
- FU2-12 / 两问自查 / ① 新增表 / 注册 / 缓存：`FrontendUnitView._grid_layout` / `FacingIndicatorView._grid_layout`（随 view `queue_free` 走）；item_preview 沙盒 instance 进 `GameWorld._instances`（reset / `_exit_tree` 两个出口对称 `destroy_instance`）；无 revoke / `remove_actor` / `shutdown` / 换 AbilitySet 相关新表。② 循环：`ProjectileSystem.tick` 仍遍历局部 `projectiles` 快照，派发发生在其中、注册表变化由趟末 `_process_pending_removal` 处理；`WorldView._on_grid_configured` 遍历 `_unit_views.values()` 只读；`_is_tile_available` 各调用方循环未变。
- FU2-13 / 范围外新发现（记 §6，未修）/ ① `u_grid_map.gd` 现为仓内零引用、无人注册的可选脚本，可删连同 README 段；② `process_post_event` 现在会在 `GameplayInstance.base_tick` 遍历 `_systems` 的过程中跑 handler，handler 里若 `add_system`（就地重排 `_systems`）会让本趟遍历漏 / 重 tick 某个 system，今日无此 handler。

## 后续观察 A1（2026-09-17，followups 第 4 轮：A1 十一条；addons `df86d90`）

- FU4-1 / 条目 1 / `TimelineData._init` 改 `tags = p_tags.duplicate()`；单测：`from_dict(payload)` 经 builder `.timeline()` 冻结后 `payload["tags"]` 仍可写、副本不跟随后续改动 / 冻结应是 TimelineData 自己的属性，不能连带冻住调用方字典。
- FU4-2 / 条目 2 / `AbilitySet.can_activate` 两条断言各改为守卫 + `denied(reason, FAILED_ABILITY)` 返回；单测：别家 ability（其门本会 allowed）与 null 各恰一条断言、都得 denied / debug 下断言不阻断，原来对 null 报引擎错误、对别家 ability 用本 set 的 owner 干跑。
- FU4-3 / 条目 3 / `smoke_world_view` 空 view 分支文案改为「bind + spawn + 动画通过；无 unit view 可供 remove_actor 验证，跳过 Step 5」，保留 `_pass` / 死者留 world 不 remove，「战斗期 remove signal 已验证」不成立；该分支只在 world 本就没有 unit 时进入。[假设：不改判定]
- FU4-4 / 条目 4 / `GameWorld.create_instance`：同一对象重复注册幂等返回、不再警告；同 id 异对象 `assert_crash` 后返回 null、不登记；`world_test` +2（幂等零警告 / 撞 id 恰一条断言 + 注册表仍是先注册者） / 静默错位改响亮；返回 null 让忽略断言的调用方下一步就炸，而不是拿着旧实例继续。[假设：返回 null 而非旧实例]
- FU4-5 / 条目 5 / hex `HexBattleProcedure` 与 `SkillPreviewProcedure` 的 `tick_once`：两个 actor 循环每个 actor 前判 `_finished` 早返回，`record_current_frame_events` 前再判；core `tick()` 注释同步；新 smoke `smoke_world_end_mid_tick`（hex/regression）两幕各先红后绿 / 战斗 tick 内 world 被结束时本 world 已出注册表，余下 actor 的 action 只会反查到 null instance；inkmon 半条冻结。
- FU4-6 / 条目 6 / `PreEventComponent` 注册 id 加本组件在 ability 里的序号（`"<ability_id>_pre_<kind>_<index>"`，`get_all_components().find(self)`）；单测：两个同 kind 组件先注销后者不误删前者 / 按 id 注销只删首个匹配；序号取组件位置，确定性、不引入计数器。
- FU4-7 / 条目 7 / 死链：根 `AGENTS.md` skill 段改指 `.claude/skills` 与 LGF `CLAUDE.md`、删 `/update-lgf-skill` 行；dota2 README 删 CHANGELOG 指针、框架文档改指 LGF `CLAUDE.md`，dota2 `CHANGELOG.md` 删（历史归 git log）；adr-0005 提案链接加已删注；lomolib spec 两处 `.agents/skills` → `.claude/skills`；task-queue 四处加已删注（该文件与 triage 文档按协作约定只在工作区追加、不进本 commit）。
- FU4-8 / 条目 8 / `maxHp` 兜底：hex render_world（含 attribute_changed 过滤）/ harness / `smoke_skill_preview_environment` 只读 `max_hp`，7 处 hex 前端 smoke 夹具改 `max_hp` / 全部 attribute config 都是 `max_hp`；inkmon render_world 半条冻结。
- FU4-9 / 条目 9 / `smoke_summon_spike` 已由第 2 轮 B-7 登记进 hex/skills 并改走 `configure_grid` + `instance.grid`，本轮只核对，无改动。
- FU4-10 / 条目 10 / stdlib grid 单测 +2：预订按 id 无条件扫（坐标无效的 IGridOccupant 与没有 hex_position 的 actor 都扫，占用只在坐标有效时看）；`actor_removed` handler 内棋盘已清且 actor 已出 registry（ONE_SHOT 连接免成环）；测试头注释同步 / 钉住 D8 合同。
- FU4-11 / 条目 11 / `EventProcessor._create_trace` trace_level 0 返回空字典（不建 10 键 dict、不生成 trace id、不 `randi()`），写点按 `tracing` 守卫，`_finalize_trace` 对空字典早返回；单测：level 0 派发中 `get_current_trace_id()` 为空、handler 照常执行，level 1 记录 pre / post 两条含 cancel 与 end_time 的 trace / 每事件固定开销；level 0 下派发中的 current trace id 由「未记录的 id」变为空串，零读者。
- FU4-12 / 验收 / `all` 130 scene 全 PASS（core 单测 242，本轮 +8；hex/regression +1）；释放测试随组绿；`hex/random-golden` 以第 2 轮重烤版通过，回退本轮三处 core 改动 A/B 复跑 10 seed sha 逐条相同（本轮零漂移）；inkmon golden 指纹不变；两问自查：无新增表 / 注册 / 缓存（注册 id 只改格式），无新增循环（`tick_once` 只加早返回）。

## 已关闭的后续观察

> 从 §6「后续观察」搬来，原文保留，末尾括注关闭依据。

- `DEFAULT_TRACE_LEVEL` 改 0 后，唯一真正的「常驻世界」反而仍显式传 1：`inkmon/host/ink_mon_world_host.gd:47` 与 13 个 inkmon smoke 都写死 `EventProcessorConfig.new(20, 1)`，改默认值没覆盖到它们。P4「inkmon 夹具收敛」已计划把这些调用点去参（`InkMonWorldGI._init` 传 `(20, 0)`），届时才真正闭环。（P4 已落地：host 与 13 个 smoke 改 `GameWorld.shutdown()`，`InkMonWorldGI._init` 固定 `EventProcessorConfig.new(20)`，trace 走默认 0，见 ㉝③，本条关闭。）
- **`refcount_release_test` 的「干净起点」依赖运行顺序**（P2 发现）：`GameWorld.init()` 只在 `_initialized` 为 true 时才 `shutdown()`，而同套件更早的 `world_test.gd` 调过 `GameWorld.destroy()` 把它置回 false——于是释放测试开头那次 `init()` 实际没清 `_instances`，只换了 processor/collector。当前无害（各测试自己 `destroy_instance`），但硬关卡的前提是靠运气成立的。P4 把 `GameWorld` 生命周期动词收成单一 `shutdown()` 时一并处理。（P4 已处理：释放测试每个用例开头调幂等、无条件清注册表的 `GameWorld.shutdown()`，本条关闭。）
- **`HexBattleGameStateUtils.is_actor_dead` 注释与实现相反**（P3 改参数名时发现）：注释写「无法获取角色则返回 false」，实际 actor 找不到时返回 `true`（只有 battle 为 null 才返回 false），`damage_action.gd` 有调用点。改注释还是改实现，要先确认调用方想要哪个。（P3 复审第三轮已删除该函数，唯一调用点改用 `target_actor.is_dead()`，本条关闭。）
- `/update-lgf-skill` 命令（`.claude/commands/update-lgf-skill.md`）需修三处：`grep -oP` 换可移植提取（如 `sed -n 's/.*"last_commit"[^"]*"\([^"]*\)".*/\1/p'`）；diff 改在 submodule 内跑（`git -C addons diff <addons-sha>..HEAD -- logic-game-framework/`）；`update.json.last_commit` 改记 addons SHA（主仓 SHA → gitlink 经历史清理后会失联）。（P5.5 已删除该命令与 `enforcing-lgf/update.json`，增量机制随 reference 文档一起退役，本条关闭。）
- **LGF 参考文档成片教不存在的 API**（P3 review 发现，复审第四轮补全清单）：① `target-selector.md`「类层次结构 / 工厂方法 / 内置选择器」三节的 `TargetSelector.CurrentTarget` / `AbilityOwner` / `Fixed` 与 `current_target()` / `ability_owner()` / `fixed()`，以及「最佳实践」里的 `TargetSelector.fixed()`，core 里都没有（`target_selector.gd` 只有基类 `select` 与 `filtered()` / `Filtered`，具体选择器在项目层如 `HexBattleTargetSelectors`）。P3 只把本阶段改到的三段自定义选择器示例换成真实 API。② `action-system.md` 除 P3 改写的「固定值 vs 执行期计算」一节外：构造模板的 `= null` 默认值与 `TargetSelector.current_target()` 兜底（真实 `_init(target_selector)` 无默认值）；新 Action 示例直接 `extends Action.BaseAction`（现行分层合同要求选 `PrimitiveAction` / `FlowActionBase` / `SkillLocalAction`，validator 会报；但 P6 会删掉前两者与 validator、改回 `extends Action.BaseAction`，以 P6 之后的合同为准）；`get_targets` 写成返回 `Array[ActorRef]`（实为 `Array[String]`）；回调示例的 `TargetSelector.*`、裸 float、未限定 `DamageType`，且 `on_critical` 没配 `emit_pre_basic_attack()`（非普攻路径永不触发）；回调模板缺 `get_child_actions()` 与 `Action.execute_child`；内置 Action 表的参数名（`damage` → `damage_resolver`、`params` → `cue_params`）；`ActionResult` 的 `events` / `metadata` / `error`（实为 `event_dicts` / `data` / `failure_reason`）。③ `docs/README.md`：「快速开始 / 基本用法」与「重要约定」2、3 节（`TargetSelector.current_target()` / `fixed([actor_ref…])`、裸 `50.0` 伤害值、`DamageType.PHYSICAL`）；§4「在 Action 中使用」的 `extends Action.BaseAction`（同 ②，P6 之后即为正确写法）；§5 流程图与代码示例（`modify_hp` 不存在、死后 `remove_actor`、`target.id`、`process_post_event` 传事件对象）；事件类设计模式 / 完整示例 / 序列化约定（hex `BattleEvents` 已是 snake_case 而示例仍写 camelCase、`class Base`；core `GameEvent` / `PlaybackData` 的 key 在 P7 之前仍是 camelCase，「序列化约定」节按 P7 文档条目改）；使用示例·逻辑层（`_resolve_target` 等零定义、`ctx.source_actor_id`、`ActionResult.success()`）与表演层（`DamageVisualizer.visualize`，实为 `FrontendDamageVisualizer.translate`）；Actor 管理架构的 `get_local_id()`；「创建 Actor」的 `GameplayInstance.create_actor()`（实为 `add_actor`）与 add 前的 id 取值；BattleActor 协议的 `CharacterActor extends BattleActor`（实为 `HexBattleActor`）；目录树。同族注释：`event_collector.gd:12/30`、`event_processor.gd:45` 的 `modify_hp`。三份都要按现状整页重写，别逐符号替换（只改一个符号会给假信心，同 `.lomo-team` 那条），并排在本计划 P9 完成之后做：P6（Action 基类层级）、P7（事件 key 大小写）、P8（hp 写入改 `set_hp` / `add_hp`）、P9（grid 出 core）都会改变这几页「按现状」该写的内容。（P5.5 已删 `target-selector.md` / `action-system.md` / `docs/README.md` 三篇，不再重写；同族 `.gd` 注释与其余指向已删文档的注释见新条目，本条关闭。）
- **context 存活没有机制级守卫**（P3 复审发现）：释放测试只在四条路径上手挂 weakref 探针（NoInstance on_apply action、trigger filter、事件 action、timeline tag action），on_remove / 叠层 / Break / cancel / can_activate / PreEvent 重建 / callback context 都不在内；任一 component 在未探路径（如 `on_stacks_changed`、on_remove）把 context 存进字段，三关照样全绿。建议 P4 C1 动 `ExecutionContext` 时或 P5 新增 `rebuild_for_handler` 构造点时落地：两类 context 加 debug 构建下的存活计数（`static var _live: int`，`_init` 加一、`NOTIFICATION_PREDELETE` 减一，body 用 `OS.is_debug_build()` 门控），释放测试按用例断言增量为 0，hex `smoke_skill_scenarios` 与 inkmon `smoke_m1_battle` 收尾断言为 0。（P4 已落地，见 ㉚，本条关闭。）
- **`.agents/skills` 镜像没有同步机制，且已有漂移**（P3 复审发现，既有）：`AGENTS.md` 把 `.agents/skills` 定为 Codex 的 skill，同步全靠每阶段手工 `cp` + `diff -r`。P3 改到的 `enforcing-lgf` / `gdscript-coding` 两棵树逐字节一致，但整体 `diff -rq .claude/skills .agents/skills` 不一致：`.agents/skills/lgf-new-logic-skill/SKILL.md` 仍要求先读已废除的 `docs/skills/skill-implementation-progress.md`，`.agents/skills/sim-nav-map-bugfix/SKILL.md` 引用不存在的 `.Codex/` 路径（另有平台措辞、Codex 专用文件等有意差异）。建议另开 chore：先修这两处陈旧副本，再加按 skill 白名单的镜像检查，不做 junction。（P5.5 已删 `.agents/skills` 全部 11 个 skill，镜像机制退役，本条关闭；根目录 `AGENTS.md` 仍提及它，见新条目。）
- **直接驱动 procedure `finish()` 后 world 仍报 `has_active_battle()`**（P3 复审发现，既有）：`_active_battle` 只在 `world.tick()` 收尾时置空，dota2 这类直接调 `procedure.finish()` 的流程之后若在同一 world 再 `start_battle` 会撞断言。㉒ 拆掉了 world ↔ procedure 的强引用环（不再泄漏），状态位本身仍是陈旧的。（P4 复审第 1 轮已修：`finish()` / `abort()` 自己交还槽位，释放测试用例 3 断言，见 ㉟，本条关闭。）
- **开着录像的战斗中途 destroy 仍泄漏 recorder 与它订阅的全部 actor**（P3 复审第三轮发现，既有；第四轮更正范围与拆环点）：`BattleRecorder.actor_subscriptions` 里的退订闭包读 actor 成员（强持 actor），actor 上的监听闭包捕获 `RecordingContext → _recorder`。订阅范围不是 `_participant_ids`，而是开战时 `world.get_recordable_actors()` 全体（`battle_procedure.gd:60`）加战斗中经 `actor_added` 补录的 spawn：hex 不覆盖 `should_record_actor`，中途召唤的图腾 / 火焰地形也在内；inkmon 录全部 `InkMonUnitActor`，含出战队（`battle_roster_slice`）之外的 roster；`ProjectileActor` 的 `setup_recording` 返回空，不被钉住。拆环点是一次退订全部的 `stop_recording`（调用方为 `procedure.finish()` 与 hex demo / inkmon GI 的 `get_replay_data()`，后者在战斗中调用会提前停录像）和逐 actor 的 `unregister_actor`（全仓零调用），而 `GameplayInstance.end()` / `destroy_instance` / `shutdown` 都不终止进行中的战斗（实例：inkmon `smoke_world_command` 的 `request_training_battle` → `tick_once` → `destroy_all_instances`）。修向：`WorldGameplayInstance.on_end()` 在 despawn 之前做**不发信号**的 abort（`get_is_recording()` 为真才停 recorder——`get_replay_data()` 可能已停过，无守卫再调会 push_error "Not recording"；按订阅表退订全部，不按 `_participant_ids`；断开 `actor_added`、清 `_active_battle`）——不能直接 `finish()`，那会存日志、写回放、改 inkmon 任务状态。挂 P4 C2（`shutdown()` 统一后中途拆除会变成常规路径），释放用例形状见 P4 循环引用要点。（P4 已修，见 ㉙，本条关闭。）
- **`GameWorld.create_instance(factory)` 的回调间接层已无用处**（P3 复审第三轮发现）：全仓 factory 只有「`return X.new()`」「配好 grid 就返回」「把早已建好的实例原样返回」三种，都不需要延迟构造，却留着「注册前干活」的位置，契约只能靠注释守。P4 C2 重写 `GameWorld` 生命周期时顺手改成 `create_instance(instance)`（注册后返回）。（P4 已改，见 ㉛，本条关闭。）
- **测试入口不因 SCRIPT ERROR 判负**（P4 复审第 2 轮发现）：`TestFramework` 只数断言失败与零断言，`tools/run_tests.ps1` 只看退出码、超时与 `SMOKE_TEST_RESULT`；GDScript 运行期错误（含 `Log.assert_crash` 的断言）只中止出错那一帧、调用方照常往下走——「降级不报错」「断言不触发」这类合同在全仓测试里都可能假绿。P4 只在 `instance_context_test` 与释放测试用例 4 用本地 `Logger` 计数器（`tests/log_counter.gd`）补了断言；验收第 1 关靠人工扫日志兜底（当前 86 scene 零 SCRIPT ERROR）。修向：launcher 的 Finish-Scene 在日志含 `SCRIPT ERROR:` 时判 FAIL，或 `TestFramework._run_test` 逐用例挂计数器——两者都波及全部测试组，需要单独一轮评估。（P5.5 已落地：`tools/run_tests.ps1` 的 `Finish-Scene` 遇 `SCRIPT ERROR:` 判 FAIL、reason 带首条匹配行，先红后绿验证过，86 scene 零 SCRIPT ERROR 基线成立，本条关闭。）
- `AbilityConfig.collect_timelines()` 只认 `components` 里的 `ActivateInstanceConfig`，而 `ActiveUseConfig extends AbilityComponentConfig`（**不**继承前者）——技能若写成 `.component_config(ActiveUseConfig.builder()…build())` 而非 `.active_use(…)`，其 timeline 对 lint 完全隐形。仓内当前无此写法；P6「`ActiveUseConfig extends ActivateInstanceConfig`」落地后自动消解。（P6 已落地：`ActiveUseConfig extends ActivateInstanceConfig`，两种写法都进同一个 `components` 列表、都被 `collect_timelines` 认到，本条关闭。）
- **`.gd` 注释仍指向 P5.5 已删文档**（P5.5 发现，本阶段不改 `.gd`）：指向 `docs/README.md` 的 `world_gameplay_instance.gd` / `battle_procedure.gd` / `playback_data.gd` / hex `world_view.gd` / `battle_animator.gd` / `skill_preview_world.gd` / `hex_demo_world_gameplay_instance.gd`；指向 `docs/reference/action-architecture.md` 的 `Action.gd` / `execution_context.gd` / `action_architecture_validator.gd`；指向 hex `docs/reference/view-logic-reconciliation.md` 的 `demo_frontend.gd` / `view_logic_reconciler.gd` / `smoke_frontend_main.gd` / `skill_preview.gd` / `hex_world_gameplay_instance.gd`；指向 `shield-system.md` 的 `thorn.gd`；`skill_preview.gd` 两处「CHANGELOG 待处理 / D5」。另 `event_collector.gd` / `event_processor.gd` 注释里的 `modify_hp` 不存在。P6（`Action.gd` 等）/ P9（`world_gameplay_instance.gd`）触及时改指规则之家，其余 P10 收口一并清。（P6 已改 `Action.gd` / `execution_context.gd`，`action_architecture_validator.gd` 已删；P9 改 `world_gameplay_instance.gd` 等 5 处；P10 清完其余 14 处，本条关闭。）
- **`AbilityLifecycleContext` 三处手拼、字段来源不一**（P3 复审发现，既有）：`AbilitySet._create_lifecycle_context` 用 `get_event_processor()`、`Ability._build_remove_context` 传 null、`PreEventComponent._rebuild_context` 用 `GameWorld.event_processor`——on_remove / Break / 叠层钩子拿到的 `event_processor` 恒为 null（今天零读者）。P4 C1 可顺手改成从 `instance` 派生的只读属性（与 `ExecutionContext.event_collector` 同形），三处连同 P5 的 `rebuild_for_handler` 收成一个工厂。相邻的既有问题：被 Break 的 ability 其 pre handler 仍会改事件（`_rebuild_context` 与 `PreHandlerRegistration` 都不查 `is_disabled()`），设计意图未核实。（P4 已把 `event_processor` 改为派生只读属性，见 ㉗；P5 把两个按 owner 反查的构造收进 `AbilityLifecycleContext.for_ability` / `rebuild_for_handler`，`AbilitySet` 保留直接构造，见 ㊸；Break 这条用户 2026-09-13 拍板不做——Break 技能未继续设计，今天只影响 hex 代达罗斯暴击，见 P10.5-4，本条关闭。）
- **注册进 GameWorld 之前 grant 的纯 post 被动永久缺订阅**（P5 审查发现，计划如此）：`Ability._register_post_handlers` 在 context 没有 processor 时静默返回；P5 之前广播在派发时按 id 反查，注册前 grant 无害。「EventProcessor not available」探针只覆盖带 `PreEventComponent` 的 ability，纯 NoInstance / ActivateInstance 的被动（荆棘、亡语）没有信号。仓内生产路径都先注册再 grant。修向：processor 缺席时与 `PreEventComponent.on_apply` 同样打警告，或 `grant_ability` 断言 owner 已注册（孤立单测要一起改）。（2026-09-14 已落地：`grant_ability` 断言 owner 已登记进 instance，孤立单测改为先 `add_actor`，见 P10.5-1，本条关闭。）
- **post handler 里过期的 ability 不被当场 revoke**（P5 审查发现）：post 派发直接调 `Ability.receive_event`，不经 `AbilitySet._process_abilities` 的过期清扫；一次性被动在自己触发的 action 里 `expire()` 之后，以 EXPIRED 状态留在 set 里，直到下一次经过这个 AbilitySet 的 tick 或定向投递（grant、激活请求）才被清扫，期间 `has_ability` 仍命中、`abilityRemoved` 延后。hex 只在护盾破裂一处当场 revoke。修向：`Ability._make_post_handler` 在 `receive_event` 之后发现过期即 `context.ability_set.revoke_ability(...)`，或让 `expire` 通知所属 set。（2026-09-14 已落地：`Ability._make_post_handler` 在 `receive_event` 后发现过期即 `revoke_ability`，见 P10.5-2，本条关闭。）
- **`Ability.owner_actor_id` 与所在 AbilitySet 的 owner 是两个来源**（P5 复审发现，既有）：pre / post 注册取 set 的 owner（lifecycle context），`AbilityLifecycleContext.for_ability` 与 `AbilityRef.from_ability` 取 `Ability.owner_actor_id`。构造时传的 owner 与被 grant 进的 set 不一致时，注册与派发正常，on_remove / 叠层 / Break 钩子与 execution 却反查不到 instance（`TagComponent` 的 tag 撤不掉、timeline action 拿到 null instance）。生产里 `Ability.new` 都传被 grant 的 actor 的 id。修向：`AbilitySet.grant_ability` 断言两者相等，或把 ability 的 owner 同步成 set 的 owner。（2026-09-14 已落地：`grant_ability` 盖章——构造留空即填 set 的 owner、非空不一致即断言，见 P10.5-3；「id 由工厂派发」的更大重构列候选刀，见 §6 后续观察，本条关闭。）
- **`StatModifierComponent.on_remove` / `DynamicStatModifierComponent` 直接解引用 `context.attribute_set`**（P2 发现）：同文件的 `on_passive_disabled` / `on_passive_enabled` 都有 `if context.attribute_set == null: return` 守卫，`on_remove` 没有。鸭子探测改真类型后，「子类忘了覆盖 `get_attribute_set()`」从「有字段就命中」变成「静默 null」，这里会在 buff 被 revoke 那一刻才炸。没顺手加守卫是因为加了会把崩溃换成「modifier 没被移除」的静默泄漏，取舍要单独想清楚。（2026-09-17 B-6 已落地：三个钩子共用 `_raw_attribute_set_or_assert`，null 即断言报明原因、仍清内部记录，两个组件各钉一条「销毁后 revoke 恰一条断言」用例，见 FU1-4，本条关闭。）
- **`Actor.serialize()` 全仓零生产消费者**（P2 发现）：录像走 `PlaybackData.ActorInitData.create`，inkmon 存档走 `to_dict()`。P2 把三份子类实现收敛进 `BattleActor.serialize()` 并加了 `is_dead` 键，等于给一条无人调用的路径加维护面；dota2 还因此白得一个不含 `position_2d` 的 `serialize()`。要么找到真实用途（debug dump / inspector），要么整条删掉。（2026-09-17 B-4 已落地：整条删除，规则之家加「可序列化挂读者不挂类」，见 FU1-3，本条关闭。）
- **`GameWorld.destroy_all_instances()` 与 `shutdown()` 行为已完全相同**（P4 发现）：两者都是「结束全部 instance + 清注册表」，只差日志级别（debug / info）。计划的保留清单两个都列了；inkmon host 的 `reset_session` / `load_game` 与 5 个 inkmon smoke 用前者，场景收尾用后者。要么删 `destroy_all_instances` 统一成一个动词，要么给两者定下可区分的语义。（2026-09-17 B-3 已落地：删 `destroy_all_instances`、21 处调用改 `shutdown()`，合同放宽为「任何要干净注册表的时刻」，见 FU1-2，本条关闭。）
- **core 没有「整个换掉 AbilitySet」的出口**（P5 审查发现）：inkmon 为同一根因手写两处清理——`InkMonUnitActor.reset_battle_runtime` 按 owner 注销 handler、`InkMonUnitActor._clear_equipment_abilities` 按 source 清装备 modifier，其余 `on_apply` 留下的外部状态换集时照样残留。修向：`AbilitySet.revoke_all(reason)`，换集前调它。（2026-09-17 B-1 已落地：core 出口是 `revoke_abilities_where(predicate, reason)` 而非 `revoke_all`——条件由游戏层写；inkmon `reset_battle_runtime` 换集前调它与删两处手写清理归 inkmon 重设计（task-queue 2e），见 FU1-1，本条关闭。）
- **item_preview 的沙盒 instance 没注册进 GameWorld**（P3 发现）：`item_preview.gd` 用 `GameplayInstance.new(...)` 直接建沙盒再 `add_actor`，没走 `GameWorld.create_instance`。装备容器的 grant 走 `GameWorld.get_actor(owner)`，在这个沙盒里恒找不到 owner（打 `Log.error` 后跳过 grant）；P3 之后 context 的 instance 也按 id 反查，同样拿不到。现象早于 P3，`smoke_item_preview_boot` 不覆盖装备 grant 路径——要么沙盒改走 `create_instance`，要么明确 item preview 不演示装备 ability。（2026-09-17 B-5 已落地：沙盒经 `GameWorld.create_instance` 登记、reset / 退出 `destroy_instance`，见 FU2-8，本条关闭。）
- **投射物 HIT / MISS 的 post 派发仍由 example 层回扫 collector**（P5 审查发现）：hex procedure、skill-preview procedure 与测试 harness 各自手动调 `HexWorldGameplayInstance.broadcast_projectile_events`；观众参数删掉之后，stdlib `ProjectileSystem` 可以在产生事件处直接派发。（2026-09-17 B-2 已落地：stdlib `ProjectileSystem` 产出即派发，三个回扫调用点删除，golden 按解释重烤，见 FU2-1–3，本条关闭。）
- **hex 仍有两条棋盘真相**（P9 发现，计划如此）：`ai_strategy.gd` / `angle_cone.gd` / `facing_indicator_view.gd` / `skill_preview.gd` 直接读 `UGridMap.model`，harness 的 `_create_actor` / `_create_environment` 经 `UGridMap.model.place_occupant` 放人——与 `world.grid` 是同一对象，靠 `HexWorldGameplayInstance.configure_grid` 的 UGridMap 桥保持一致。inkmon 已零引用 UGridMap；hex 若也退 autoload，这几处要改走 `world(ctx).grid`。（P10 整体审补充：桥让棋盘寿命超出 world——`SkillPreviewWorldGI.reset()` 清 registry 并 `grid = null` 但不清占用、`_systems.clear()` 不经 `on_unregister`，hex world 结束后 `UGridMap.model` 仍钉住整张旧棋盘与全部 occupant actor 到下一次 `configure_grid` / 进程退出；单槽位不累积，low。）（2026-09-17 B-7 已落地：hex 退 `UGridMap` autoload，AI / 锥形 / 前端 / 预览 / harness 全部改走 world 的 grid，插件不再注册 autoload，见 FU2-4–7，本条关闭。）
- **`smoke_summon_spike.tscn` 不在任何 test group**（P9 发现）：四个阶段各自 `instance.grid = UGridMap.model` 直赋绕过 `configure_grid`（不发 `grid_configured`），且从不随 launcher 跑。要么登记进 hex 组并改走 `configure_grid`，要么删。（2026-09-17 B-7 随手：改走 `configure_grid` / `instance.grid`，登记进 `hex/skills`，见 FU2-7，本条关闭。）
- `TimelineData._init` 把传入的 `tags` **按引用**存下（`timeline_data.gd:20`），而 builder `.timeline(data)` 现在会 `make_read_only()` 它——若某处用 `TimelineData.from_dict(payload)` 建 timeline，`payload["tags"]` 会被就地冻结，调用方后续改它会报 read-only。仓内所有 timeline 都用内联字面量声明，属潜在而非现症；根治是 `_init` 里 `tags = p_tags.duplicate()`（或按 altitude 角度建议把冻结整个下沉进 `_init`，让「不可变」成为类型固有属性而非 builder 副作用）。（2026-09-17 A1 已落地：`_init` 改 `tags = p_tags.duplicate()`，单测钉住 builder 冻结不再波及调用方字典，见 FU4-1，本条关闭。）
- **`AbilitySet.can_activate` 断言之后照常往下走**（P3 review 发现，既有）：两条 `Log.assert_crash`（ability 非空、属于本 set）在 debug 下不阻断——传 null 时第二条断言格式化 `ability.id` 先报引擎错误、把第一条盖掉；传别家的 ability 时照样用本 set 的 owner 建 context 干跑并返回结果。只影响报错质量，不影响「查询零副作用」。（2026-09-17 A1 已落地：两条断言后各返回 `denied(FAILED_ABILITY)`，null / 别家 ability 不再干跑，见 FU4-2，本条关闭。）
- **dota2 冷却期每 tick 空发激活请求**（P3 复审发现，既有）：`Dota2AutoBattleProcedure` 的 5d 只跳过执行中的单位、不跳过冷却中的，交战单位约每秒 17–19 次 `ABILITY_ACTIVATE` 被 `NoTagCondition` 拒绝，每次 push `AbilityActivateFailed`（进 `Dota2LogicFrame`，挤 14 行 debug 面板）并打 debug 日志（smoke 日志里「条件不满足」1220 行、「开始执行」85 行）。修向：请求前用零副作用的 `ability_set.can_activate(ability)` 预检。另：`EventProcessor._create_trace` 在 trace_level 0 时仍无条件分配 10 键 Dictionary，是每个事件的主要固定开销。（2026-09-17：预检半条已随 B-10 落地——`_request_basic_attack` 请求前 `can_activate` 预检，smoke 断言整场 `ability_activate_failed` 为零，见 deviations FU2-9；`_create_trace` 分配半条仍开，归 A1。）（2026-09-17 A1：`_create_trace` 半条已落地——trace_level 0 返回空字典不再分配，见 FU4-11；预检半条见 FU2-9，本条关闭。）
- **hex 伤害流程参考文档仍写「死后 remove_actor」**（P3 复审第四轮发现，既有，2026-04-26「死者留 world」起过期）：`example/hex-atb-battle/docs/reference/damage-pipeline.md` 流程图 ⑧ 与「关键顺序约束」表两行（「on_break 在 remove_actor 之前，否则拿不到 owner 上下文」「post_event 在 remove_actor 之后，需要尸体已不在战场」）、`shield-system.md:43/48/185`。代码注释已在 ㉔ 改正；这两份要按「死亡检测锁存 `is_dead()` 并清 grid 占用，位置与 ability 仍在」重写顺序约束的**理由**，不是换词。`smoke_world_view.gd` 空 view 分支的通过文案（「战斗期 remove signal 已验证」）同样过期。（P5.5 两份文档已删，顺序约束的理由已按「死亡锁存 `is_dead()` + 清 grid 占用、不 remove_actor」改写进 hex `README.md` 设计铁律；`smoke_world_view.gd` 文案仍开。）（2026-09-17 A1：`smoke_world_view.gd` 空 view 分支文案已改为「无 view 可供 remove_actor 验证、跳过 Step 5」，见 FU4-3，本条关闭。）
- **`GameWorld.create_instance` 撞 id 时返回旧实例**（P4 发现，既有语义）：警告后返回已注册的那个；改成 instance 形态后调用方多半丢弃返回值、继续用手里那个没注册进去的新实例，而 context 反查到的是旧实例——静默错位。P4 未改语义。修向：同 id 异对象 `Log.assert_crash`，同一对象重复注册保持幂等。（2026-09-17 A1 已落地：同对象幂等返回、同 id 异对象 `assert_crash` 后返回 null 不登记，`world_test` +2，见 FU4-4，本条关闭。）
- **战斗 tick 内结束 world 时，正在执行的 `tick_once()` 仍跑完函数体**（P4 复审第 2 轮发现）：`WorldGameplayInstance.tick()` 只在 `tick_once()` 返回后才发现槽位已交还；hex / inkmon / skill-preview 的 `tick_once` 只在入口判一次 `_finished`，余下的 actor 循环照常执行，此时注册表已注销该 world、`ctx.instance` 反查为 null，走 `HexBattleGameStateUtils.world(ctx)` 的 hex action 会逐个断言。生产代码没有在战斗 tick 里结束 world 的路径（inkmon `smoke_world_command` 是先 `tick_once` 再在外层 `destroy_all_instances`）。修向：procedure 的 actor 循环里每个 actor 前再判 `_finished`。（2026-09-17 A1：hex / skill-preview 两个 procedure 已在每个 actor 前判 `_finished` 早返回，新 smoke `smoke_world_end_mid_tick` 钉住；inkmon `InkMonBattleProcedure` 半条随 inkmon 冻结归重设计（task-queue 2e），见 FU4-5，本条关闭。）
- **同一 ability 挂两个同 kind 的 `PreEventComponent` 时注册 id 相同**（P5 发现，既有）：id 是 `"<ability_id>_pre_<kind>"`，按 id 注销会删掉先匹配到的那一条（可能是另一个 component 的）。post 按 kind 去重、每种 kind 一条，没有这个问题。（2026-09-17 A1 已落地：注册 id 带组件序号 `"<ability_id>_pre_<kind>_<index>"`，单测钉住互不误删，见 FU4-6，本条关闭。）
- **处置表之外仍指向已删文档 / 已删目录的文件**（P5.5 发现，按「不在表内一律不动」未动）：根 `AGENTS.md`（`.agents/skills/` 与 `/update-lgf-skill`）、dota2 `README.md`（`docs/README.md` / `action-architecture.md`）与 dota2 `CHANGELOG.md`（同样该随「历史归 git log」退役）、`docs/future/task-queue.md`（proposals / `docs/README.md`）、`docs/adr/0005`（proposals，历史记录可留）、`addons/lomolib/docs/dev-agent-debug-mode-spec.md`（`.agents/skills`）。（2026-09-17 A1 已落地：六处全部处置——`AGENTS.md` / dota2 README 改指、dota2 CHANGELOG 删、adr-0005 与 task-queue 加已删注、lomolib spec 改指 `.claude/skills`，见 FU4-7，本条关闭。）
- **`maxHp` 属性名兜底只剩夹具在用**（P7 发现）：hex / inkmon 两份 `render_world.gd`、`skill_scenario_harness`、`smoke_skill_preview_environment` 都写 `attributes.get("maxHp", attributes.get("max_hp", …))`，而所有 attribute config 定义的都是 `max_hp`，`maxHp` 只出现在 hex 前端 smoke 手写的 `actor_init.attributes` 夹具里。可删兜底、夹具改 `max_hp`。（2026-09-17 A1：hex render_world / harness / `smoke_skill_preview_environment` 只读 `max_hp`，7 处 hex 前端 smoke 夹具改 `max_hp`；inkmon render_world 半条随 inkmon 冻结（task-queue 2e），见 FU4-8，本条关闭。）
- **stdlib grid 单测未钉两条 D8 合同**（P10 整体审，low）：`tests/stdlib/grid/grid_world_gameplay_instance_test.gd` 里没有 `hex_position` 的 `plain` 不持预订，「预订按 id 无条件扫（坐标无效也扫）」与「`remove_actor` 先清足迹再出 registry（`actor_removed` handler 内读棋盘已是清后状态）」两条只存在于实现，改回旧形状测试仍绿。（2026-09-17 A1 已落地：两条合同各一用例钉住，见 FU4-10，本条关闭。）
