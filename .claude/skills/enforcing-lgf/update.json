{
  "last_commit": "8af9821",
  "last_update": "2026-09-11",
  "note": "LGF core 重构 P4（事件设施归 GameplayInstance）手动增量：last_commit 记 addons SHA。本轮更新 SKILL.md §4 与 reference/{abilities,actions,conventions-detail,entity,events,stdlib,example-app-game-logic}.md：GameplayInstance 自持 event_processor / event_collector，ExecutionContext.event_collector 与 AbilityLifecycleContext.event_processor 为派生只读属性；GameWorld 收成注册表（shutdown / create_instance(instance)）；BattleRecorder 注入 collector + get_event_collector、RecordingContext 两参、ProjectileSystem 从 get_instance() 取 collector；WorldGameplayInstance.end() 中止进行中的战斗、finish() / abort() 交还战斗槽位、BattleProcedure.abort() / BattleRecorder.abort_recording()；context 存活计数 get_live_count()；EventProcessor.set_trace_level()。/update-lgf-skill 命令本机仍不可用，此处为手动等价执行。"
}
